---
phase: 04-langgraph-langsmith
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - examples/ch04_langgraph/01_state_graph.py
  - examples/ch04_langgraph/02_graph_mermaid.py
  - outputs/ch04_langgraph/01_state_graph.out
  - outputs/ch04_langgraph/02_graph_mermaid.out
  - book/src/ch04_langgraph/01_state_graph.md
  - book/src/SUMMARY.md
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-05]

must_haves:
  truths:
    - "Chapter 4-1 shows, in real captured output, a state built from TypedDict + Annotated[list, add_messages] and that MessagesState is that same shape, with the add_messages reducer appending messages (GRAPH-01)"
    - "The same capture shows StateGraph nodes/edges: START -> model, a conditional edge from model to tools or END, and a tools -> model loop-back edge (GRAPH-01)"
    - "Running the Phase 2 baseline question through the graph reproduces the Phase 2 manual loop exactly: parallel lookup_stock('사과')/lookup_stock('바나나'), multiply(a=12, b=5), message flow Human->AI->Tool->Tool->AI->Tool->AI, and the final answer string identical to the Phase 2 baseline, asserted in the output as a printed True (GRAPH-02)"
    - "The capture shows a tool-body exception coming back as an error ToolMessage because ToolNode is constructed with handle_tool_errors=True, and the chapter explains that the default would kill the graph"
    - "The chapter renders the graph as an actual mermaid picture on the built page, generated from draw_mermaid() and included from a capture — not hand-typed (GRAPH-05)"
    - "No UUID, tool_call id, checkpoint id, timestamp or latency appears in either capture, and a scratch re-capture of the LLM-free mermaid example is byte-identical"
  artifacts:
    - path: "examples/ch04_langgraph/01_state_graph.py"
      provides: "GRAPH-01/02 example: MessagesState + ToolNode(handle_tool_errors=True) + tools_condition loop reproducing the Phase 2 baseline"
      contains: "handle_tool_errors=True"
    - path: "examples/ch04_langgraph/02_graph_mermaid.py"
      provides: "GRAPH-05 example whose entire stdout is the draw_mermaid() source"
      contains: "draw_mermaid()"
    - path: "outputs/ch04_langgraph/02_graph_mermaid.out"
      provides: "Captured mermaid source, line 2 onward is pure diagram text"
      contains: "graph TD;"
    - path: "book/src/ch04_langgraph/01_state_graph.md"
      provides: "Chapter 4-1"
      contains: "{{#include ../../../outputs/ch04_langgraph/02_graph_mermaid.out:2:}}"
    - path: "book/src/SUMMARY.md"
      provides: "New part between 3부 and 부록"
      contains: "# 4부 LangGraph"
  key_links:
    - from: "examples/ch04_langgraph/01_state_graph.py"
      to: "examples/shared/tools.py ALL_TOOLS"
      via: "from shared.tools import ALL_TOOLS, fed to both bind_tools and ToolNode"
      pattern: "from shared\\.tools import ALL_TOOLS"
    - from: "examples/ch04_langgraph/01_state_graph.py"
      to: "examples/shared/config.py get_chat_model"
      via: "from shared.config import get_chat_model (no ChatOpenAI( outside config.py)"
      pattern: "from shared\\.config import get_chat_model"
    - from: "model node"
      to: "tools node"
      via: "add_conditional_edges(model, tools_condition, {tools: tools, END: END}) plus add_edge(tools, model) loop-back"
      pattern: "add_conditional_edges"
    - from: "book/src/ch04_langgraph/01_state_graph.md mermaid fence"
      to: "outputs/ch04_langgraph/02_graph_mermaid.out"
      via: "{{#include ...:2:}} expanded by mdBook links preprocessor, then rendered by mdbook-mermaid"
      pattern: "```mermaid"
---

<objective>
Open part 4 of the book. Rebuild Phase 2's hand-written tool loop as a `StateGraph` and prove — in the capture itself — that it produces the identical baseline result, then show the graph as a real rendered mermaid diagram.

Purpose: GRAPH-01, GRAPH-02 and GRAPH-05 are the foundation the rest of the phase stands on; 04-03's checkpointer chapters reuse this exact graph shape. The "identical to the Phase 2 baseline" claim is the pedagogical core of the part, so the example asserts it rather than leaving it to prose.
Output: 2 examples, 2 `.out`, chapter 4-1, and `# 4부 LangGraph` in SUMMARY. Committed locally; no push.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-langgraph-langsmith/04-RESEARCH.md
@.planning/phases/04-langgraph-langsmith/04-01-SUMMARY.md
@.planning/phases/02-tool-calling/02-01-SUMMARY.md
@examples/shared/config.py
@examples/shared/tools.py
@examples/ch02_tools/02_tool_loop.py
@scripts/run_examples.py
@book/src/SUMMARY.md
@book/src/ch02_tools/02_tool_loop.md
@book/src/ch03_rag/03_rag_chain.md

`02-01-SUMMARY.md` holds the baseline this plan must reproduce. Use `book/src/ch02_tools/02_tool_loop.md` as the tone/structure reference (평서체 "~다").

Facts already confirmed during planning (verified live against this repo's endpoint — do not re-derive):
- The graph below reproduces the Phase 2 baseline exactly, twice in a row: `lookup_stock('사과')` + `lookup_stock('바나나')` in parallel, then `multiply(a=12, b=5)` -> `60`, then the final answer `사과 재고 12개 × 바나나 재고 5개 = **60**입니다.`, with message flow `HumanMessage AIMessage ToolMessage ToolMessage AIMessage ToolMessage AIMessage`.
- `ToolNode(ALL_TOOLS)` with default error handling lets a `ValueError` raised inside `lookup_stock` escape and kill the graph. `ToolNode(ALL_TOOLS, handle_tool_errors=True)` converts it to `ToolMessage(status='error', content="Error: ValueError('재고 목록에 없는 품목: 수박')\n Please fix your mistakes.")`. Unknown tool names and missing arguments are already handled by the default.
- `draw_mermaid()` output is byte-identical across runs. It begins with a `---`/`config:`/`---` mermaid frontmatter block, then `graph TD;`, four node lines (`__start__`, `model`, `tools`, `__end__`), four edge lines and three `classDef` lines. Tabs indent the body.
- `{{#include}}` expands inside a ```mermaid fence; 04-01 already installed and pinned the preprocessor and added `mermaid` to `check_book.py`'s include-only languages.
- `run_examples.py` collects only `*.py` whose filename starts with a digit, from `examples/ch*/`, and runs each with cwd `examples/`.
- `book/book.toml` has `create-missing = false`: add the SUMMARY link only after the chapter `.md` exists.
- Printing `add_messages` itself (or any message object `repr`) would leak a function address or a message UUID. Print names, types, contents and counts instead.
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only (never `git add -A`/`git add .`). Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`. Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`. Never modify `examples/.env`. **Do NOT push in this plan.**
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. After `test -n "$LITELLM_API_KEY"`, check each new `.out` with `/usr/bin/grep -cF -- "$LITELLM_API_KEY" FILE` → `0` before viewing it. Checks report counts only.
3. **grep:** the shell's `grep` is a ugrep wrapper. Use `/usr/bin/grep` or `git grep`.
4. **Outputs are generated, never hand-edited.** To change an output, change the `.py` and re-run the runner.
5. **Examples:** the chat model comes only from `from shared.config import get_chat_model`; tools only from `shared.tools`. No `# ANCHOR` markers, no `load_dotenv`, no sys.path hacks, no `@tool` literal, no `ChatOpenAI(`. Short `=== N. 섹션 ===` headers — **except `02_graph_mermaid.py`, whose entire stdout must be the diagram source and nothing else**. Examples exit 0.
6. **Never print volatile values.** No UUIDs (message ids, tool_call ids, checkpoint ids), no timestamps, no latencies, no memory addresses, no absolute paths. Use `type(x).__name__`, explicit attributes, counts and booleans. Reuse Phase 2's `fmt_call(tc)` = `f"{tc['name']}({json.dumps(tc['args'], ensure_ascii=False)})"`, which prints name + args and never an id.
7. **Chapter format** (enforced by `scripts/check_book.py`): `##` headings exactly `개념: 왜 필요한가` / `최소 코드` / `실제 출력` / `요점 정리`, in that order (`###` allowed inside). Every `python`/`text`/`ini`/`mermaid` fence is a single `{{#include}}` line; `.out` includes use `:2:`; `shared/*.py` only by anchor name. Only `bash` fences may hold typed content. Never write the literal word `ANCHOR` or `source-sha256` in prose. Include paths from `book/src/ch04_langgraph/` are `../../../examples/...` and `../../../outputs/...`.
8. **Truthful prose:** write the 실제 출력 prose only after reading the captured `.out`. Phase 3's lesson applies: when output prints an indirect identifier, prose mis-describes it — so print human-readable labels in the example, and re-read the capture against the prose once before committing.
9. **Retry rule (captures):** if the graph run fails to show its point (the final answer does not match the Phase 2 baseline, or the error section does not produce an error `ToolMessage`), do NOT change the graph, the tools, `shared/config.py`, or `handle_tool_errors`. A mismatched final answer is a *finding*, not a defect to be tuned away: re-run the capture once; if it still differs, record the exact difference and report it — the chapter may then state the difference honestly instead of claiming equality. At most one such re-run.
10. **Reproducibility rule:** classify any scratch re-capture difference.
    - **Printed volatile value** (UUID, id, timestamp, latency, path, address): an example bug. Fix the print, re-capture, re-check.
    - **Wording-only** (only model-written prose differs; same tool calls, same args, same message flow, same final-answer equality result): pass, record it.
    - **Structural divergence** (different tool calls/args, different message flow, different node sequence, or the baseline-equality boolean flipping): model/library variance, not a code bug. Do not edit code. Re-run the scratch capture once more. If the second re-run matches the committed output, record the flake and continue. Otherwise STOP and report without further commits.
    `02_graph_mermaid.out` is LLM-free, so it has no wording-only case: any difference there is volatile or structural.
11. **Public-repo hygiene:** no absolute home paths, scratchpad paths, email addresses, hardware specs or backend ports in any committed file. Write `~`, `<repo>`, `<scratchpad>`. Temp files go in `$SCRATCH`. No `sleep`.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: 01_state_graph.py — state, nodes/edges, Phase 2 baseline reproduction, tool-error handling</name>
  <files>examples/ch04_langgraph/01_state_graph.py, outputs/ch04_langgraph/01_state_graph.out</files>
  <action>
Module docstring: `"""4부 1장: 2부의 수동 도구 루프를 StateGraph로 다시 만든다."""`

Imports: `json`; `Annotated`, `TypedDict` from `typing`; `HumanMessage`, `AIMessage` from `langchain_core.messages`; `StateGraph`, `START`, `END`, `MessagesState`, `add_messages` from `langgraph.graph`; `ToolNode`, `tools_condition` from `langgraph.prebuilt`; `get_chat_model` from `shared.config`; `ALL_TOOLS` from `shared.tools`.

Module level:
- a hand-written state class, kept for teaching and actually exercised in section 1:
```python
class ChatState(TypedDict):
    # add_messages는 "덮어쓰기" 대신 "이어 붙이기"로 합치는 리듀서다
    messages: Annotated[list, add_messages]
```
- `BASELINE_ANSWER = "사과 재고 12개 × 바나나 재고 5개 = **60**입니다."` with a comment: 2부 수동 루프가 낸 최종 답 (02-01-SUMMARY.md 기록)
- `QUESTION = "사과 재고와 바나나 재고를 곱하면 몇이야? 재고 조회 후 곱셈 도구로 계산해줘."`
- `fmt_call(tc)` copied from `examples/ch02_tools/02_tool_loop.py`
- `build_graph()` returning the compiled app:
```python
def build_graph():
    model_with_tools = get_chat_model().bind_tools(ALL_TOOLS)

    def call_model(state: MessagesState) -> dict:
        return {"messages": [model_with_tools.invoke(state["messages"])]}

    graph = StateGraph(MessagesState)
    graph.add_node("model", call_model)
    # handle_tool_errors=True가 없으면 도구 함수 안에서 난 예외가 그래프를 그대로 죽인다
    graph.add_node("tools", ToolNode(ALL_TOOLS, handle_tool_errors=True))
    graph.add_edge(START, "model")
    graph.add_conditional_edges("model", tools_condition, {"tools": "tools", END: END})
    graph.add_edge("tools", "model")  # 도구 결과를 들고 모델로 되돌아가는 루프백
    return graph.compile()
```
- `describe(messages)` helper that prints one line per message: `AIMessage` → `AI: tool_calls [fmt_call, ...]` or `AI: {content}`; `ToolMessage` → `Tool[{m.name}] ({m.status}): {m.content}`; `HumanMessage` → `Human: {m.content}`. Print the tool *name* alongside every tool result — never rely on position or an id (Phase 3's lesson).

Sections:
1. `=== 1. 상태(State): TypedDict + add_messages ===`
   - print `ChatState 필드: {list(ChatState.__annotations__)}` and `MessagesState 필드: {list(MessagesState.__annotations__)}` (both `['messages']`) with a printed note that `MessagesState`는 위 `ChatState`와 같은 모양을 미리 만들어 둔 것
   - exercise the reducer with **explicit ids so nothing random is generated**, and print only types/contents:
```python
old = [HumanMessage("안녕", id="m1")]
new = [AIMessage("반가워", id="m2")]
merged = add_messages(old, new)
print(f"add_messages([1개], [1개]) -> {len(merged)}개: {[type(m).__name__ for m in merged]}")
```
   - print a one-line explanation that a node returns only the *new* messages and the reducer appends them
2. `=== 2. 노드와 엣지 ===`
   - `app = build_graph()`
   - `g = app.get_graph()`
   - print `노드: {sorted(g.nodes)}`
   - print each edge on its own line, sorted for determinism: `for src, dst, cond in sorted((e.source, e.target, bool(e.conditional)) for e in g.edges): print(f"  {src} -> {dst}" + ("  (조건부)" if cond else ""))`
   - print a line stating the loop-back edge exists: `tools -> model 루프백 있음: {("tools", "model", False) in ...}` — compute it from the same sorted tuple list so it can never disagree with the lines above
3. `=== 3. 2부 도구 루프를 그래프로 재현 ===`
   - print `질문: {QUESTION}`
   - `result = app.invoke({"messages": [HumanMessage(QUESTION)]})`
   - `describe(result["messages"])`
   - print `메시지 흐름: ` + `" → ".join(type(m).__name__ for m in result["messages"])`
   - `final = result["messages"][-1].content`
   - print `최종 답: {final}`
   - print `2부 수동 루프의 최종 답과 동일: {final == BASELINE_ANSWER}`  ← this printed boolean is the GRAPH-02 proof
4. `=== 4. 도구 안에서 난 예외도 ToolMessage로 ===`
   - `result = app.invoke({"messages": [HumanMessage("수박 재고를 확인해줘.")]})`
   - `describe(result["messages"])` (the `ToolMessage` line prints `(error)` as its status and the error text)
   - print `마지막 답: {result["messages"][-1].content}`
   - print one line noting this only works because the tools node was built with `handle_tool_errors=True`

Capture: `uv run --project examples python scripts/run_examples.py ch04_langgraph/01_state_graph.py` (with warm-up — this example calls the LLM).

Commit: `feat(04-02): StateGraph reconstruction of the manual tool loop`. Stage `examples/ch04_langgraph/01_state_graph.py outputs/ch04_langgraph/01_state_graph.out`.
  </action>
  <verify>
- The runner prints `OK`. Key count on `outputs/ch04_langgraph/01_state_graph.out` is `0`. No `Traceback`, no `0x`, no `call_` id strings, no `/Users/` in it.
- Section 2 shows nodes `['__end__', '__start__', 'model', 'tools']`, an edge `__start__ -> model`, conditional edges from `model`, and `tools -> model`; the loop-back boolean prints `True`.
- Section 3 shows: `lookup_stock({"item": "사과"})` and `lookup_stock({"item": "바나나"})` on the first AI turn, `multiply({"a": 12, "b": 5})` on the second, tool results `사과 재고: 12개` / `바나나 재고: 5개` / `60`, the flow `HumanMessage → AIMessage → ToolMessage → ToolMessage → AIMessage → ToolMessage → AIMessage`, and **`2부 수동 루프의 최종 답과 동일: True`**. If that boolean prints `False`, apply rule 9 (re-run once, then report honestly — do not tune).
- Section 4 contains `(error)` and `재고 목록에 없는 품목: 수박`, and the run still exits 0 (the graph did not die).
- `git grep -n "ChatOpenAI(" -- examples` matches only `examples/shared/config.py`; `git grep -n "@tool" -- examples ':!examples/.venv'` matches only `examples/shared/tools.py`.
  </verify>
  <done>One graph reproduces the Phase 2 manual loop exactly — same tool calls, same arguments, same message flow, and an asserted-in-output identical final answer — and shows a tool-body exception returning as an error `ToolMessage` instead of killing the graph. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: 02_graph_mermaid.py — capture the diagram source as pure output</name>
  <files>examples/ch04_langgraph/02_graph_mermaid.py, outputs/ch04_langgraph/02_graph_mermaid.out</files>
  <action>
Module docstring: `"""4부 1장: 같은 그래프의 구조를 mermaid 다이어그램 소스로 출력한다."""`

This file exists as its own example for one reason, which belongs in a comment at the top of the script: its **entire stdout is the diagram source**, so the capture can be included directly into a ```mermaid fence and rendered as a picture. Any `=== 섹션 ===` header would end up inside the diagram and break it.

Copy `build_graph()` verbatim from `01_state_graph.py` (same imports it needs: `StateGraph`, `START`, `END`, `MessagesState`, `ToolNode`, `tools_condition`, `get_chat_model`, `ALL_TOOLS`) with a comment noting it is intentionally identical to 1장's graph — the same verbatim-copy convention 3부 used for `load_documents()`. Do not import from the other example.

Body:
```python
app = build_graph()
print(app.get_graph().draw_mermaid(), end="")
```
No other print. No LLM call happens: `bind_tools` and `compile()` do not contact the endpoint.

Capture: `uv run --project examples python scripts/run_examples.py ch04_langgraph/02_graph_mermaid.py --no-warmup` (no LLM call, so warm-up is unnecessary).

Then confirm determinism immediately: re-capture into `$SCRATCH/early` and `diff` against the committed file; require byte-identical (rule 10).

Commit: `feat(04-02): capture the graph structure as mermaid source`. Stage `examples/ch04_langgraph/02_graph_mermaid.py outputs/ch04_langgraph/02_graph_mermaid.out`.
  </action>
  <verify>
- The runner prints `OK`; key count on the `.out` is `0`.
- Line 1 of the `.out` is the generated hash header; **line 2 onward is valid, self-contained mermaid**: line 2 is `---`, the file contains `graph TD;`, the four node names `__start__`, `model`, `tools`, `__end__`, an edge `tools --> model;`, and `classDef` lines. It contains no `===`, no Korean prose, no `Traceback`.
- `uv run --project examples python -c "import pathlib; t=pathlib.Path('outputs/ch04_langgraph/02_graph_mermaid.out').read_text().splitlines(); print(t[1]); print(len(t))"` shows `---` as the first captured line (sanity that `:2:` starts the diagram, not the hash header).
- Early determinism: the scratch re-capture is byte-identical to the committed `.out`.
- `uv run --project examples python scripts/check_leaks.py outputs/ch04_langgraph` → `CLEAN`.
  </verify>
  <done>`outputs/ch04_langgraph/02_graph_mermaid.out` holds the real, byte-reproducible `draw_mermaid()` source and nothing else, ready to be included straight into a mermaid fence. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Chapter 4-1 + 4부 in SUMMARY + rendered-diagram verification</name>
  <files>book/src/ch04_langgraph/01_state_graph.md, book/src/SUMMARY.md</files>
  <action>
**A. `book/src/SUMMARY.md`:** insert between the 3부 list and `# 부록`, with blank lines around it:
```
# 4부 LangGraph

- [도구 루프를 그래프로 다시 만들기](ch04_langgraph/01_state_graph.md)
```

**B. `book/src/ch04_langgraph/01_state_graph.md`**, titled `# 도구 루프를 그래프로 다시 만들기`:

- **개념: 왜 필요한가**
  - 2부의 수동 루프는 `for`문과 `if`문으로 "모델 → 도구 → 모델"을 직접 돌렸다. 분기·반복·중단 조건이 늘어날수록 이 루프는 읽기 어려워진다.
  - LangGraph는 같은 흐름을 **상태(State) + 노드 + 엣지**로 선언한다. 상태는 누적되는 메시지 목록이고, 노드는 상태를 받아 새 메시지를 돌려주는 함수이며, 엣지는 다음에 어느 노드로 갈지를 정한다.
  - 리듀서 `add_messages`가 핵심이다: 노드는 *새로 생긴* 메시지만 돌려주고, 리듀서가 기존 목록에 이어 붙인다. 그래서 노드 함수는 전체 대화를 관리할 필요가 없다.
  - 조건부 엣지(`tools_condition`)가 2부의 `if ai_msg.tool_calls:` 자리를 대신하고, `tools → model` 루프백 엣지가 `for`문의 반복을 대신한다.
  - 이 장의 목표는 새 기능이 아니라 **같은 결과를 다른 구조로** 얻는 것이다 — 2부와 똑같은 질문에 똑같은 최종 답이 나오는지 출력으로 확인한다.
- **최소 코드**
  - `### 예제 코드`: path line, `python` include of `01_state_graph.py`, then bullets on:
    - `TypedDict` + `Annotated[list, add_messages]`, and that `MessagesState`는 그 모양을 미리 만들어 둔 것
    - `ToolNode`가 2부의 `run_tool_call`을 대신하되, **`handle_tool_errors=True`를 반드시 켜야** 도구 함수 안에서 난 예외가 `ToolMessage`로 돌아온다는 점 — 기본값에서는 예외가 그대로 올라와 그래프 실행이 멈춘다
    - `tools_condition`이 마지막 `AIMessage`에 `tool_calls`가 있으면 `tools`로, 없으면 `END`로 보낸다는 점
    - `add_edge("tools", "model")` 루프백이 반복을 만든다는 점, 그리고 2부의 `MAX_ITERS` 자리에는 `recursion_limit`이 있다는 예고(다음 장)
    - `compile()`이 실행 가능한 앱을 만든다는 점
    Then `실행 방법 (examples 디렉터리에서):` and a `bash` fence `uv run python ch04_langgraph/01_state_graph.py`.
  - `### 그래프를 그림으로 그리는 코드`: one sentence that `draw_mermaid()`는 컴파일된 그래프에서 mermaid 소스를 만들어 준다, then the `python` include of `02_graph_mermaid.py`, then a note that 이 예제의 출력 전체가 다이어그램 소스이므로 책에 그대로 그림으로 실린다, plus the `bash` fence `uv run python ch04_langgraph/02_graph_mermaid.py`.
- **실제 출력**
  - The standard LLM-capture sentence used in 2부/3부 (실제 실행 결과이며 LLM 응답이라 글자 단위로 같지 않을 수 있다), then the `text` fence with `{{#include ../../../outputs/ch04_langgraph/01_state_graph.out:2:}}`.
  - Interpret strictly from the capture: 상태 필드 이름, `add_messages`가 1개+1개를 몇 개로 만들었는지, 노드 목록과 엣지(조건부 엣지와 루프백을 짚어서), 3번 섹션의 도구 호출 순서와 인자, 메시지 흐름, 그리고 **`2부 수동 루프의 최종 답과 동일: True`가 뜻하는 것** — 2부 2장의 최종 답과 글자까지 같다는 뜻임을 명시. 4번 섹션은 `(error)` 상태의 `ToolMessage`가 모델에게 전달되어 그래프가 죽지 않고 답을 만들었다는 점.
  - `### 그래프 다이어그램`: one sentence that 아래 그림은 위 예제가 출력한 mermaid 소스를 그대로 렌더링한 것이다, then the fence:
    ````
    ```mermaid
    {{#include ../../../outputs/ch04_langgraph/02_graph_mermaid.out:2:}}
    ```
    ````
    Then read the diagram against the capture and describe it: `__start__`에서 `model`로 들어가고, `model`에서 점선(조건부)으로 `tools` 또는 `__end__`로 갈라지며, `tools`에서 실선으로 `model`로 돌아오는 고리가 보인다는 것.
- **요점 정리**: 3–5 bullets. End with a plain-text (no link) sentence that 다음 장에서는 이 그래프에 체크포인터를 붙여 대화를 기억시키고 `recursion_limit`으로 무한 루프를 막는다.

**C. Verify the diagram actually renders** (not just that the source is present) — this is the whole point of 04-01's toolchain work:
- `rm -rf book/book && mdbook build book`
- In `book/book/ch04_langgraph/01_state_graph.html`: assert `<pre class="mermaid">` is present, that the block body contains `graph TD;` and `tools --> model`, that the page has a `src="mermaid-<hash>.min.js"` and a `src="mermaid-init-<hash>.js"` script tag, and that the page contains neither `{{#include` nor a literal ```mermaid fence.
- Assert the referenced hashed asset files exist under `book/book/` and the bundle is >1,000,000 bytes.

Commit: `feat(04-02): chapter 4-1 (도구 루프를 그래프로 다시 만들기)`. Stage `book/src/ch04_langgraph/01_state_graph.md book/src/SUMMARY.md`.
  </action>
  <verify>
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`; the only Warning line is the known mdbook-mermaid version-mismatch line.
- `uv run --project examples python scripts/check_book.py --html book/book` → 11× `PASS`, exit 0 (10 existing + chapter 4-1).
- Rendered-diagram assertions from C all hold.
- `/usr/bin/grep -c '4부 LangGraph' book/book/toc.html` ≥ 1.
- `uv run --project examples python scripts/check_leaks.py book/src/ch04_langgraph outputs/ch04_langgraph` → `CLEAN`.
- Prose/capture cross-check (rule 8): re-read `outputs/ch04_langgraph/01_state_graph.out` line by line against the 실제 출력 paragraphs; every number, tool name, argument and boolean mentioned in prose appears in the capture with that exact value.
  </verify>
  <done>Chapter 4-1 is live in the local build under `# 4부 LangGraph`: it explains state/nodes/edges, shows the graph reproducing the Phase 2 baseline with an asserted-identical final answer, shows tool errors returning as `ToolMessage`, and displays the graph as an actually-rendered mermaid picture generated from a capture. 11 chapters pass. Committed locally, not pushed.</done>
</task>

</tasks>

<verification>
Phase 4 success criteria covered here:
1. `TypedDict`/`add_messages` state and `StateGraph` nodes/edges are visible in real output, and the graph is shown as a mermaid diagram (criterion 1 — GRAPH-01, GRAPH-05)
2. The `add_conditional_edges` + loop-back graph produces the same final answer as Phase 2's manual loop, proven by a printed equality boolean (criterion 2 — GRAPH-02)
3. Invariants: single chat-model factory, tools only from `shared/tools.py`, no volatile values in captures, byte-identical LLM-free re-capture, include-only chapter (11× PASS)
</verification>

<success_criteria>
- GRAPH-01, GRAPH-02 and GRAPH-05 are satisfied locally with real, reproducible captures and a genuinely rendered diagram
- The graph shape in `build_graph()` is the stable base 04-03 reuses for checkpointers
- Nothing pushed; all work committed on local main
</success_criteria>

<output>
After completion, create `.planning/phases/04-langgraph-langsmith/04-02-SUMMARY.md`. Record, with no secret values, absolute paths, email addresses or ports:
- the exact final answer captured and whether the baseline-equality boolean printed `True` (if `False`, the exact difference and what the chapter says instead)
- the tool calls and arguments observed per step, and the message flow line
- the error-section `ToolMessage` status and content, and the model's follow-up answer
- the mermaid capture's node and edge lines, and confirmation that the built page contained `<pre class="mermaid">` plus both hashed asset references
- the early determinism result for `02_graph_mermaid.out`, and any rule-10 classification applied to `01_state_graph.out`
Before committing SUMMARY, check that `/usr/bin/grep -cE '/Users/[A-Za-z]|/private/|/tmp/claude' .planning/phases/04-langgraph-langsmith/04-02-SUMMARY.md` prints `0`.
</output>
