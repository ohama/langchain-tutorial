---
phase: 04-langgraph-langsmith
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - examples/ch04_langgraph/03_checkpointer.py
  - examples/ch04_langgraph/04_sqlite_checkpoint.py
  - examples/ch04_langgraph/sqlite_worker.py
  - outputs/ch04_langgraph/03_checkpointer.out
  - outputs/ch04_langgraph/04_sqlite_checkpoint.out
  - book/src/ch04_langgraph/02_checkpointer.md
  - book/src/ch04_langgraph/03_sqlite_checkpoint.md
  - book/src/SUMMARY.md
autonomous: true
requirements: [GRAPH-03, GRAPH-04, GRAPH-06]

must_haves:
  truths:
    - "Chapter 4-2 shows, in real captured output, the same question asked twice without a checkpointer and the model not remembering, then the same two turns with InMemorySaver and one thread_id where the model does remember (GRAPH-03)"
    - "The same capture shows a different thread_id not sharing that memory, and the stored message count growing across turns within a thread (GRAPH-03)"
    - "The same capture shows a deliberately cyclic pure-Python graph stopped by recursion_limit=5 with the real GraphRecursionError message, with no LLM involved (GRAPH-04)"
    - "Chapter 4-3 shows two separate OS processes, launched by subprocess, sharing one SqliteSaver file: the second process answers from state the first process wrote, proving restart-survival rather than in-process reuse (GRAPH-06)"
    - "The SQLite demo deletes the .db, .db-wal and .db-shm files at start and end, prints no filesystem path, and leaves git status clean"
    - "Neither capture prints a checkpoint id, thread-internal UUID, timestamp or latency"
  artifacts:
    - path: "examples/ch04_langgraph/03_checkpointer.py"
      provides: "GRAPH-03 + GRAPH-04 example: InMemorySaver/thread_id memory and a deterministic recursion_limit trip"
      contains: "InMemorySaver"
    - path: "examples/ch04_langgraph/04_sqlite_checkpoint.py"
      provides: "GRAPH-06 driver: cleans the db, launches two worker processes, cleans up again"
      contains: "subprocess.run"
    - path: "examples/ch04_langgraph/sqlite_worker.py"
      provides: "One-turn worker run as a separate OS process against the shared SqliteSaver file"
      contains: "SqliteSaver.from_conn_string"
    - path: "book/src/ch04_langgraph/02_checkpointer.md"
      provides: "Chapter 4-2"
      contains: "{{#include ../../../outputs/ch04_langgraph/03_checkpointer.out:2:}}"
    - path: "book/src/ch04_langgraph/03_sqlite_checkpoint.md"
      provides: "Chapter 4-3"
      contains: "{{#include ../../../outputs/ch04_langgraph/04_sqlite_checkpoint.out:2:}}"
  key_links:
    - from: "examples/ch04_langgraph/03_checkpointer.py"
      to: "compiled graph with memory"
      via: "graph.compile(checkpointer=InMemorySaver()) invoked with config={'configurable': {'thread_id': ...}}"
      pattern: "thread_id"
    - from: "examples/ch04_langgraph/04_sqlite_checkpoint.py"
      to: "examples/ch04_langgraph/sqlite_worker.py"
      via: "subprocess.run([sys.executable, worker, turn, db_path, thread_id, parent_pid]) twice"
      pattern: "sys\\.executable"
    - from: "examples/ch04_langgraph/sqlite_worker.py"
      to: "shared SQLite checkpoint file"
      via: "with SqliteSaver.from_conn_string(db_path) as saver: graph.compile(checkpointer=saver)"
      pattern: "from_conn_string"
---

<objective>
Give the 4부 graph a memory and a brake. Chapter 4-2 adds `InMemorySaver` + `thread_id` so a conversation continues across turns (and shows a second thread not sharing it), then stops a deliberately infinite graph with `recursion_limit`. Chapter 4-3 proves durability the only way that actually proves it: two separate OS processes sharing one SQLite checkpoint file.

Purpose: GRAPH-03, GRAPH-04 and GRAPH-06. These are also the mechanisms Phase 5's capstone agent relies on for multi-turn work, so the examples are written to be reused.
Output: 3 example files (2 captured + 1 worker), 2 `.out`, chapters 4-2 and 4-3, 2 more SUMMARY links. Committed locally; no push.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-langgraph-langsmith/04-RESEARCH.md
@.planning/phases/04-langgraph-langsmith/04-02-SUMMARY.md
@examples/ch04_langgraph/01_state_graph.py
@examples/shared/config.py
@examples/shared/tools.py
@scripts/run_examples.py
@book/src/SUMMARY.md
@book/src/ch04_langgraph/01_state_graph.md

`04-02-SUMMARY.md` records the graph shape and capture conventions this plan reuses. Chapter 4-1 is the tone reference.

Facts already confirmed during planning (verified live — do not re-derive):
- The pure-Python cyclic graph below raises `GraphRecursionError` deterministically, byte-identically across runs, with no LLM involved. Its message is exactly:
  `Recursion limit of 5 reached without hitting a stop condition. You can increase the limit by setting the 'recursion_limit' config key.` followed by a second line `For troubleshooting, visit: https://docs.langchain.com/oss/python/langgraph/errors/GRAPH_RECURSION_LIMIT` (the config key is in backticks in the real text). The URL is official documentation, not a secret, and passes the leak scan.
- `SqliteSaver.from_conn_string(db_path)` is a context manager; `setup()` (schema creation) is called lazily and automatically — never call it by hand.
- `SqliteSaver` uses `PRAGMA journal_mode=WAL`, so `<db>`, `<db>-wal` and `<db>-shm` all appear. 04-01 gitignored all three; this plan also deletes all three at start and end.
- Two separate `uv run` processes sharing one db file do carry conversation state across a real process boundary (verified: the second process answered from a name the first process stored).
- `run_examples.py` collects only `*.py` whose filename **starts with a digit**, so `sqlite_worker.py` is deliberately *not* collected and produces no `.out` of its own. It is still included into the chapter as source.
- `run_examples.py` runs each example with cwd `examples/`, capturing stdout only; a subprocess's stdout must be forwarded by the parent to appear in the capture.
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only (never `git add -A`/`git add .`). Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`, and never any `*.db*` artifact. Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`. Never modify `examples/.env`. **Do NOT push in this plan.**
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. After `test -n "$LITELLM_API_KEY"`, check each new `.out` with `/usr/bin/grep -cF -- "$LITELLM_API_KEY" FILE` → `0` before viewing it. Checks report counts only.
3. **grep:** the shell's `grep` is a ugrep wrapper. Use `/usr/bin/grep` or `git grep`.
4. **Outputs are generated, never hand-edited.**
5. **Examples:** the chat model comes only from `from shared.config import get_chat_model`; tools only from `shared.tools`. No `# ANCHOR` markers, no `load_dotenv`, no sys.path hacks, no `@tool` literal, no `ChatOpenAI(`. Short `=== N. 섹션 ===` headers. Examples exit 0 — including the recursion-limit section, whose exception must be caught.
6. **Never print volatile values.** No checkpoint ids, thread-internal UUIDs, message ids, timestamps, latencies, memory addresses, **process ids**, or **filesystem paths** (the db path is passed around but never printed). Print counts, names, contents and booleans. To prove two processes are distinct without printing a pid, have the parent pass its own pid as an argument and let the worker print the *comparison result* (`부모 프로세스와 동일: False`).
7. **Chapter format** (enforced by `scripts/check_book.py`): `##` headings exactly `개념: 왜 필요한가` / `최소 코드` / `실제 출력` / `요점 정리`, in that order. Every `python`/`text`/`ini`/`mermaid` fence is a single `{{#include}}` line; `.out` includes use `:2:`; `shared/*.py` only by anchor name. Only `bash` fences may hold typed content. Never write the literal word `ANCHOR` or `source-sha256` in prose.
8. **Truthful prose:** write the 실제 출력 prose only after reading the captured `.out`, and re-read the capture against the prose once before committing (Phase 3's lesson). Describe model answers as "이번 캡처에서는 …".
9. **Retry rule (captures):** if a capture fails to show its point (the memory turn does not recall the name, or the second thread accidentally does), change ONLY the question/name text in that example and re-capture. At most two revisions per example. Do not change the graph, `shared/config.py`, the checkpointer choice, or `recursion_limit`. If it still fails, stop and report.
10. **Reproducibility rule:** classify any scratch re-capture difference.
    - **Printed volatile value** (checkpoint id, UUID, timestamp, latency, path, pid, address): an example bug. Fix the print, re-capture, re-check.
    - **Wording-only** (only model-written prose differs; same memory/no-memory outcomes, same counts, same recursion-limit text): pass, record it.
    - **Structural divergence** (a thread remembering when it should not or vice versa, different message counts, different `GraphRecursionError` text, different node sequence): model/library variance, not a code bug. Do not edit code. Re-run the scratch capture once more. If the second re-run matches, record the flake and continue. Otherwise STOP and report without further commits.
    The recursion-limit section is LLM-free and must be byte-identical; any drift there is volatile or structural.
11. **Public-repo hygiene:** no absolute home paths, scratchpad paths, email addresses, hardware specs or backend ports in any committed file. Write `~`, `<repo>`, `<scratchpad>`. Temp files go in `$SCRATCH`. No `sleep`. Never leave a `.db*` artifact behind.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: 03_checkpointer.py + chapter 4-2 (체크포인터와 recursion_limit)</name>
  <files>examples/ch04_langgraph/03_checkpointer.py, outputs/ch04_langgraph/03_checkpointer.out, book/src/ch04_langgraph/02_checkpointer.md, book/src/SUMMARY.md</files>
  <action>
**A. `examples/ch04_langgraph/03_checkpointer.py`** — docstring `"""4부 2장: 체크포인터로 대화를 기억하고, recursion_limit으로 무한 루프를 멈춘다."""`

Imports: `TypedDict`, `Annotated` (only if needed), `HumanMessage`; `StateGraph`, `START`, `END`, `MessagesState`; `ToolNode`, `tools_condition`; `InMemorySaver` from `langgraph.checkpoint.memory`; `GraphRecursionError` from `langgraph.errors`; `get_chat_model`; `ALL_TOOLS`.

Module level: `build_graph_builder()` returning the **uncompiled** `StateGraph` (same nodes/edges as 1장, copied verbatim with a comment that it is intentionally identical), so this file can compile it twice — once without and once with a checkpointer. Also `INTRO = "내 이름은 영희야. 기억해줘."` and `RECALL = "내 이름이 뭐라고 했지?"`, and a helper `last_answer(result)` returning `result["messages"][-1].content`.

Sections:
1. `=== 1. 체크포인터가 없으면 기억하지 못한다 ===`
   - `app = build_graph_builder().compile()`
   - invoke with `INTRO`, print `1턴 질문/답변`; invoke separately with `RECALL`, print `2턴 질문/답변`
   - print `두 번째 호출이 받은 메시지 수: {len(result["messages"])}` (2 — only the new turn) with a printed note that 각 호출이 빈 상태에서 시작한다
2. `=== 2. InMemorySaver + thread_id로 기억하기 ===`
   - `app = build_graph_builder().compile(checkpointer=InMemorySaver())`
   - `config = {"configurable": {"thread_id": "chat-1"}}`
   - invoke `INTRO` with config, print the answer; invoke `RECALL` with the same config, print the answer
   - print `thread_id=chat-1에 쌓인 메시지 수: {len(app.get_state(config).values["messages"])}` — only the count from `get_state`, never the snapshot itself (it carries checkpoint ids)
   - print `답변에 '영희'가 들어있나: {"영희" in answer}` ← the explicit, human-readable memory proof
3. `=== 3. thread_id가 다르면 기억도 다르다 ===`
   - `other = {"configurable": {"thread_id": "chat-2"}}`; invoke `RECALL` on the same app with `other`
   - print the answer and `답변에 '영희'가 들어있나: {"영희" in answer}` (expected `False`) and the message count for that thread
4. `=== 4. recursion_limit으로 무한 루프 멈추기 ===`
   - a self-contained, LLM-free cyclic graph:
```python
class CountState(TypedDict):
    count: int

def bump(state: CountState) -> dict:
    return {"count": state["count"] + 1}

def always_bump(state: CountState) -> str:
    return "bump"  # END로 가는 길이 없다 — 일부러 만든 무한 루프

counter = StateGraph(CountState)
counter.add_node("bump", bump)
counter.add_edge(START, "bump")
counter.add_conditional_edges("bump", always_bump, {"bump": "bump"})
counter_app = counter.compile()
```
   - print `기본 recursion_limit은 25이고, 여기서는 5로 줄여서 빠르게 확인한다`
   - `try: counter_app.invoke({"count": 0}, config={"recursion_limit": 5})` / `except GraphRecursionError as e:` → print `{type(e).__name__}` then `{e}`
   - print a closing line that 2부의 `MAX_ITERS` 가드가 여기서는 `recursion_limit`이라는 점

Capture: `uv run --project examples python scripts/run_examples.py ch04_langgraph/03_checkpointer.py` (LLM used in sections 1–3).

**B. `book/src/SUMMARY.md`:** add `- [체크포인터와 recursion_limit](ch04_langgraph/02_checkpointer.md)` as the second 4부 link.

**C. `book/src/ch04_langgraph/02_checkpointer.md`**, titled `# 체크포인터와 recursion_limit`:
- **개념: 왜 필요한가:** 그래프는 한 번의 `invoke`가 끝나면 상태를 버린다 — 멀티턴 대화를 이어가려면 상태를 어딘가에 저장해야 한다. 체크포인터가 각 단계의 상태를 저장하고, `thread_id`가 "어느 대화인지"를 가리킨다. `InMemorySaver`는 프로세스 메모리에만 저장하므로 프로세스가 끝나면 사라진다(다음 장에서 파일로 옮긴다). 폐기된 `ConversationBufferMemory` 대신 이 방식이 표준이라는 점도 짚는다. 그리고 루프백 엣지가 있는 그래프는 원리상 끝나지 않을 수 있으므로 `recursion_limit`이 안전장치가 된다(기본값 25).
- **최소 코드:** `### 예제 코드` + path + `python` include + bullets on `compile(checkpointer=...)`, `config={"configurable": {"thread_id": ...}}`, `get_state`로 쌓인 메시지 수 확인, 같은 앱이라도 `thread_id`가 다르면 다른 대화가 된다는 점, 4번 섹션의 그래프는 LLM을 전혀 쓰지 않는 순수 파이썬 그래프라 매번 같은 오류를 낸다는 점. Then the `bash` fence `uv run python ch04_langgraph/03_checkpointer.py`.
- **실제 출력:** the standard capture sentence, the `text` include with `:2:`, then interpretation from the actual capture: 1번에서 모델이 이름을 기억하지 못한 것, 2번에서 같은 `thread_id`로 이름을 기억한 것과 쌓인 메시지 수, 3번에서 다른 `thread_id`가 기억하지 못한 것, 4번의 `GraphRecursionError` 메시지가 말하는 것(한도에 도달했고 `recursion_limit` 설정으로 늘릴 수 있다).
- **요점 정리:** 3–5 bullets, ending with a plain-text sentence that 다음 장에서는 이 기억을 파일에 저장해 프로세스를 재시작해도 남게 만든다.

Commit: `feat(04-03): chapter 4-2 (체크포인터와 recursion_limit)`.
  </action>
  <verify>
- The runner prints `OK`; key count on `outputs/ch04_langgraph/03_checkpointer.out` is `0`; no `Traceback` (the error is caught), no `0x`, no `/Users/`, no `checkpoint_id`.
- Section 2 prints `답변에 '영희'가 들어있나: True`; section 3 prints `False`. Otherwise apply rule 9.
- Section 2's message count is greater than section 3's count for the fresh thread.
- Section 4 contains `GraphRecursionError` and `Recursion limit of 5 reached without hitting a stop condition.`; the script still exits 0.
- `git grep -n "ChatOpenAI(" -- examples` matches only `examples/shared/config.py`.
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`.
- `uv run --project examples python scripts/check_book.py --html book/book` → 12× `PASS`.
- Prose/capture cross-check per rule 8.
  </verify>
  <done>Chapter 4-2 shows, from one capture, a graph forgetting without a checkpointer, remembering with `InMemorySaver` on one `thread_id`, not remembering on another, and a deliberately infinite graph stopped by `recursion_limit` with the real error text. 12 chapters pass. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: 04_sqlite_checkpoint.py + sqlite_worker.py + chapter 4-3 (프로세스 재시작)</name>
  <files>examples/ch04_langgraph/04_sqlite_checkpoint.py, examples/ch04_langgraph/sqlite_worker.py, outputs/ch04_langgraph/04_sqlite_checkpoint.out, book/src/ch04_langgraph/03_sqlite_checkpoint.md, book/src/SUMMARY.md</files>
  <action>
**A. `examples/ch04_langgraph/sqlite_worker.py`** — docstring `"""4부 3장: 별도 프로세스로 실행되어 SQLite 체크포인터에 한 턴을 기록한다."""`

Deliberately **not** numbered, so the capture runner never runs it standalone. Add a comment saying exactly that.

`argv`: `turn_label`, `db_path`, `thread_id`, `parent_pid`.

Body:
- build the same graph builder as 1장/2장 (copied verbatim, with the same "intentionally identical" comment)
- `with SqliteSaver.from_conn_string(db_path) as saver:` → `app = builder.compile(checkpointer=saver)`; `config = {"configurable": {"thread_id": thread_id}}`
- choose the question from `turn_label`: `"run1"` → `"내 이름은 철수야. 기억해줘."`, `"run2"` → `"내 이름이 뭐라고 했지?"`
- print, in this order: `[{turn_label}] 부모 프로세스와 동일: {os.getpid() == int(parent_pid)}`, `[{turn_label}] 질문: {question}`, `[{turn_label}] 답변: {answer}`, `[{turn_label}] 이 스레드에 쌓인 메시지 수: {len(app.get_state(config).values["messages"])}`
- never print `db_path`

**B. `examples/ch04_langgraph/04_sqlite_checkpoint.py`** — docstring `"""4부 3장: 프로세스를 두 번 띄워 SQLite 체크포인터가 재시작을 견디는지 확인한다."""`

Imports: `os`, `subprocess`, `sys`, `Path`.

Module level:
- `HERE = Path(__file__).resolve().parent`
- `DB_PATH = HERE / ".checkpoint_demo.db"` (dot-prefixed: 생성물임을 표시; gitignored by 04-01)
- `SIDECARS = [DB_PATH, Path(str(DB_PATH) + "-wal"), Path(str(DB_PATH) + "-shm")]`
- `WORKER = HERE / "sqlite_worker.py"`
- `THREAD_ID = "restart-demo"`
- `def clean(): [p.unlink(missing_ok=True) for p in SIDECARS]`
- `def db_exists() -> bool: return DB_PATH.exists()`
- `def run_worker(label): subprocess.run([sys.executable, str(WORKER), label, str(DB_PATH), THREAD_ID, str(os.getpid())], check=True)` — inherit stdout so the worker's lines land in the capture; do not capture and re-print (that would risk re-ordering).

Sections:
1. `=== 1. 깨끗한 상태에서 시작 ===` — `clean()`, print `시작 전 DB 파일 존재: {db_exists()}` (False), plus a line explaining WAL이 `-wal`/`-shm` 사이드카를 함께 만든다는 점 and that 경로는 출력하지 않는다
2. `=== 2. 첫 번째 프로세스 ===` — `run_worker("run1")`, then print `실행 후 DB 파일 존재: {db_exists()}` (True)
3. `=== 3. 두 번째 프로세스 (완전히 새 파이썬 프로세스) ===` — `run_worker("run2")`, then print `같은 thread_id로 이어서 대화했다: {THREAD_ID}`
4. `=== 4. 정리 ===` — `clean()`, print `정리 후 DB 파일 존재: {db_exists()}` (False) and a line that 예제는 실행할 때마다 깨끗한 상태에서 시작한다

Wrap sections 2–4 so `clean()` still runs if a worker fails (`try/finally`), and let a worker failure surface as a non-zero exit so the runner reports `FAIL` rather than silently producing a half-capture.

Capture: `uv run --project examples python scripts/run_examples.py ch04_langgraph/04_sqlite_checkpoint.py` (workers call the LLM).

**C. `book/src/SUMMARY.md`:** add `- [SQLite 체크포인터와 프로세스 재시작](ch04_langgraph/03_sqlite_checkpoint.md)` as the third 4부 link.

**D. `book/src/ch04_langgraph/03_sqlite_checkpoint.md`**, titled `# SQLite 체크포인터와 프로세스 재시작`:
- **개념: 왜 필요한가:** `InMemorySaver`는 프로세스가 끝나면 사라진다. 실제 에이전트는 껐다 켜도 하던 일을 이어가야 한다. `SqliteSaver`는 같은 상태를 파일에 저장하므로 프로세스가 바뀌어도 `thread_id`만 같으면 이어진다. 그리고 왜 "같은 프로세스 안에서 SqliteSaver 인스턴스를 새로 만드는 것"으로는 증명이 안 되는지 — 그건 재시작이 아니므로 — 이 장은 진짜로 파이썬 프로세스를 두 번 띄운다. 스키마는 첫 사용 시 자동 생성되고, WAL 모드라 사이드카 파일이 함께 생긴다는 점도 짚는다.
- **최소 코드:** `### 한 턴을 실행하는 워커` (path + `python` include of `sqlite_worker.py`, bullets on `from_conn_string` 컨텍스트 매니저와 `thread_id`), then `### 두 번 띄우는 실행기` (path + `python` include of `04_sqlite_checkpoint.py`, bullets on `subprocess.run([sys.executable, ...])`가 별도 OS 프로세스를 만든다는 점, 시작·종료 시 DB와 사이드카를 지워 매 실행이 같은 결과를 내게 한다는 점, 경로와 프로세스 번호를 출력하지 않는 이유). Then the `bash` fence `uv run python ch04_langgraph/04_sqlite_checkpoint.py`.
- **실제 출력:** the standard capture sentence + `text` include with `:2:`, then interpretation: 시작 전 DB가 없다가 첫 프로세스 후 생겼다는 것, `부모 프로세스와 동일: False`가 두 번 다 찍혀 워커가 실제로 다른 프로세스였다는 것, 두 번째 프로세스가 첫 프로세스에서 저장한 이름을 답했다는 것, 메시지 수가 턴을 거치며 늘어난 것, 마지막에 파일이 정리된 것.
- **요점 정리:** 3–5 bullets, ending with a plain-text sentence that 다음 장에서는 이 그래프가 안에서 무슨 일을 하는지 트레이싱으로 들여다본다.

Commit: `feat(04-03): chapter 4-3 (SQLite 체크포인터와 프로세스 재시작)`.
  </action>
  <verify>
- The runner prints `OK`; key count on `outputs/ch04_langgraph/04_sqlite_checkpoint.out` is `0`; no `Traceback`, no `0x`, no `/Users/`, and **no `.db` path string** in it.
- The `.out` shows `시작 전 DB 파일 존재: False`, `실행 후 DB 파일 존재: True`, `정리 후 DB 파일 존재: False`.
- `부모 프로세스와 동일: False` appears exactly twice.
- `[run2] 답변:` contains `철수`; `[run2]`'s message count is greater than `[run1]`'s. Otherwise apply rule 9.
- No artifact survived: `ls examples/ch04_langgraph/ | /usr/bin/grep -c '\.db'` prints `0`, and `git status --porcelain --ignored examples/ch04_langgraph | /usr/bin/grep -ciE '\.db(-wal|-shm)?$'` prints `0`.
- `git status --porcelain` shows no untracked files except orchestrator-owned `.planning/` docs.
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`.
- `uv run --project examples python scripts/check_book.py --html book/book` → 13× `PASS`.
- `uv run --project examples python scripts/check_leaks.py outputs/ch04_langgraph book/src/ch04_langgraph examples/ch04_langgraph` → `CLEAN`.
- Prose/capture cross-check per rule 8.
  </verify>
  <done>Chapter 4-3 proves restart-survival with two genuinely separate OS processes sharing one SQLite checkpoint file — the second answers from state the first wrote — while printing no path, pid or checkpoint id and leaving no `.db*` artifact behind. 13 chapters pass. Committed locally, not pushed.</done>
</task>

</tasks>

<verification>
Phase 4 success criteria covered here:
1. `InMemorySaver` + `thread_id` continues a conversation, and `recursion_limit` stops an infinite graph — both as real captured runs (criterion 3 — GRAPH-03, GRAPH-04)
2. The SQLite checkpointer example survives a real process restart (criterion 4 — GRAPH-06)
3. Invariants: no volatile values, no leaked paths, no leftover database artifacts, include-only chapters (13× PASS)
</verification>

<success_criteria>
- GRAPH-03, GRAPH-04 and GRAPH-06 are satisfied locally with real, reproducible captures
- The repository is byte-for-byte clean of SQLite artifacts after the capture
- Nothing pushed; all work committed on local main
</success_criteria>

<output>
After completion, create `.planning/phases/04-langgraph-langsmith/04-03-SUMMARY.md`. Record, with no secret values, absolute paths, email addresses or ports:
- the memory outcomes: whether `영희` appeared for `chat-1` and not for `chat-2`, and the message counts per thread
- the exact `GraphRecursionError` message captured
- the SQLite demo's three DB-existence booleans, the two `부모 프로세스와 동일` values, `[run2]`'s verbatim answer and both turns' message counts
- any rule-9 question revisions (old → new) and the rule-10 classification per capture
- confirmation that no `.db`, `.db-wal` or `.db-shm` file remained and `git status --porcelain` was clean
Before committing SUMMARY, check that `/usr/bin/grep -cE '/Users/[A-Za-z]|/private/|/tmp/claude' .planning/phases/04-langgraph-langsmith/04-03-SUMMARY.md` prints `0`.
</output>
