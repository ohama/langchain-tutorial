---
phase: 04-langgraph-langsmith
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/pyproject.toml
  - examples/uv.lock
  - .gitignore
  - scripts/check_book.py
  - scripts/check_leaks.py
  - scripts/test_check_leaks.py
  - book/book.toml
  - book/mermaid.min.js
  - book/mermaid-init.js
  - .github/workflows/deploy.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "`uv run --project examples python -c \"import langgraph.checkpoint.sqlite\"` succeeds on Python 3.14, and langgraph / langgraph-checkpoint-sqlite / langsmith are declared directly in examples/pyproject.toml with a passing `uv lock --check`"
    - "A ```mermaid fence is now format-gated: check_book.py rejects a hand-typed diagram and accepts only a single {{#include}} line, and all 10 existing chapters still PASS"
    - "check_leaks.py accepts repeatable --exclude GLOB, skips only matching files, still reports everything else, and the behaviour is covered by tests that fail if --exclude is removed"
    - "`mdbook build book` renders a ```mermaid fence into `<pre class=\"mermaid\">` and emits the two mermaid assets into book/book, with the mdbook-mermaid version-mismatch warning as the only WARN line"
    - "The committed book/mermaid.min.js and book/mermaid-init.js match the sha256 values produced by the pinned mdbook-mermaid 0.17.0"
    - "deploy.yml installs the pinned mdbook-mermaid release binary before `mdbook build`, using only curl/tar (no python/uv), so the CI-stays-mdBook-only invariant still holds"
  artifacts:
    - path: "examples/pyproject.toml"
      provides: "Direct declarations for langgraph, langgraph-checkpoint-sqlite, langsmith"
      contains: "langgraph-checkpoint-sqlite"
    - path: "book/book.toml"
      provides: "mermaid preprocessor registration + additional-js"
      contains: "[preprocessor.mermaid]"
    - path: "book/mermaid.min.js"
      provides: "Vendored mermaid bundle from pinned mdbook-mermaid 0.17.0"
    - path: "book/mermaid-init.js"
      provides: "Theme-aware mermaid.initialize bootstrap"
      contains: "mermaid.initialize"
    - path: "scripts/check_leaks.py"
      provides: "--exclude GLOB for vendored third-party assets"
      contains: "--exclude"
    - path: "scripts/test_check_leaks.py"
      provides: "Tests proving --exclude skips only what it matches"
    - path: ".github/workflows/deploy.yml"
      provides: "Pinned mdbook-mermaid install step before the build step"
      contains: "MDBOOK_MERMAID_VERSION"
  key_links:
    - from: "book/book.toml"
      to: "mdbook-mermaid binary"
      via: "[preprocessor.mermaid] command = \"mdbook-mermaid\""
      pattern: "command = \"mdbook-mermaid\""
    - from: "book/book.toml"
      to: "book/mermaid.min.js, book/mermaid-init.js"
      via: "[output.html] additional-js (mdBook hashes these into book/book at build time)"
      pattern: "additional-js"
    - from: ".github/workflows/deploy.yml"
      to: "github.com/badboy/mdbook-mermaid release tarball"
      via: "curl | tar -xz into RUNNER_TEMP, added to GITHUB_PATH before mdbook build"
      pattern: "mdbook-mermaid-v\\$\\{MDBOOK_MERMAID_VERSION\\}"
---

<objective>
Build the Phase 4 toolchain before any chapter exists: add the LangGraph/SQLite-checkpointer/LangSmith dependencies, ignore the SQLite checkpoint artifacts, extend the two gate scripts so a rendered mermaid diagram can be format-gated and so a vendored minified asset cannot drown the secret scanner, and install + pin the mdbook-mermaid rendering pipeline (locally and in CI).

Purpose: 4부 1장 renders a real `draw_mermaid()` diagram as a picture (user decision, GRAPH-05). That requires a preprocessor, two committed assets and a CI install step, plus two small gate-script changes. Doing this first means 04-02..04-04 are pure authoring plans, and a toolchain failure cannot leave half-written chapters behind.
Output: 3 new deps + lock, `.gitignore` rule, `mermaid` added to the include-only languages, `check_leaks.py --exclude` + tests, `book.toml` + 2 vendored assets, a pinned CI step. No chapters, no captures, no push.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-langgraph-langsmith/04-RESEARCH.md
@examples/pyproject.toml
@scripts/check_book.py
@scripts/check_leaks.py
@scripts/masking.py
@scripts/test_masking.py
@book/book.toml
@.github/workflows/deploy.yml

04-RESEARCH.md is the primary input, but this plan **overrides its Pattern 5 recommendation**: the user decided mermaid must be rendered as a real diagram (option b), not shipped as text only. Everything below was verified live during planning on this machine.

Facts already confirmed during planning (do NOT re-derive, just re-verify where a <verify> says so):
- `mdbook-mermaid` 0.17.0 and `cargo` are already on this machine's PATH (under `~/.cargo/bin`). No local install task is needed. Never write the absolute home path anywhere; write `~/.cargo/bin`.
- `{{#include}}` **does** expand inside a ```mermaid fence: mdBook's `links` preprocessor runs before `mdbook-mermaid`, so the fence body becomes the captured diagram text and is rendered as `<pre class="mermaid">…</pre>`. Verified with a scratch book.
- mdBook 0.5.3 **content-hashes** `additional-js` filenames. `mermaid.min.js` is emitted as `mermaid-eefea253.min.js` and `mermaid-init.js` as `mermaid-init-ccf746f1.js`, and the page references the hashed names. Any check for the literal string `mermaid.min.js` in rendered HTML will therefore fail — match `mermaid-<hash>.min.js` instead.
- `mdbook-mermaid install book` merges `additional-js` into the existing `[output.html]` table (preserving `site-url`, `git-repository-url`, `edit-url-template`, themes and `[output.html.search]`) and appends an empty `[preprocessor]` table plus `[preprocessor.mermaid] command = "mdbook-mermaid"`. Rehearsed against a copy of the real `book/book.toml`.
- `mdbook build` with this preprocessor prints exactly one benign warning on stderr: `Warning: The mdbook-mermaid preprocessor was built against version 0.5.0 of mdbook, but we're being called from version 0.5.3`. It is not an mdBook ERROR and does not fail CI (the CI build step greps only for `ERROR`).
- `scripts/check_book.py` currently **skips** any fence whose language is not in `INCLUDE_ONLY_LANGS = ("python", "text", "ini")`, so a hand-typed ```mermaid diagram would pass unnoticed today. Adding `"mermaid"` to that tuple turns the fence into an enforced include-only block — a strengthening, and it is satisfied by the `.out` include this phase uses.
- **The vendored `mermaid.min.js` (2,667,011 bytes) trips `check_leaks.py` with 2 `literal-secret` findings.** This is a false positive: `collect_secrets()` collects exactly one secret in this environment and it is **5 characters, alphanumeric**, occurring 73 times by coincidence in a 2.6 MB minified bundle. Do NOT "fix" this by shortening, filtering or thresholding `collect_secrets()` — that would stop masking the author's real key in captured outputs. The correct fix is a path-scoped `--exclude` plus a sha256 integrity pin on the vendored file, both in this plan.
- `langsmith==0.12.4`, `langgraph==1.2.11`, `langgraph-checkpoint==4.2.0` are already installed transitively; `langgraph-checkpoint-sqlite` is **not** installed (`ModuleNotFoundError`).
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only (never `git add -A`/`git add .`). Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`. Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`; the orchestrator owns them. Never modify `examples/.env`. **Do NOT push in this plan** — 04-04 pushes the whole phase at once.
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. Checks report counts only. Never print the value of any secret while investigating the mermaid bundle false positive — report lengths and counts only, as this plan already does.
3. **grep:** the shell's `grep` is a ugrep wrapper. Use `/usr/bin/grep` or `git grep`.
4. **Do not weaken a security gate to make it pass.** `--exclude` must be opt-in per invocation and path-scoped; `masking.py` must not change at all in this plan.
5. **Public-repo hygiene:** no absolute home paths, scratchpad paths, email addresses, hardware specs or backend ports in any committed file, including this plan's SUMMARY. Write `~`, `~/.cargo/bin`, `<repo>`, `<scratchpad>`. Temp files go in `$SCRATCH`, never the repo. No `sleep`.
6. **Reproducibility rule (applies to every plan in this phase):** a scratch re-capture that differs from a committed `.out` gets classified.
   - **Printed volatile value** (UUID, tool_call id, checkpoint id, timestamp, latency, path, memory address): an example bug. Fix the print, re-capture, re-check.
   - **Wording-only** (only model-written prose differs, same structure/facts): pass, record it.
   - **Structural divergence** (different node sequence, tool args, token counts, chunk counts, ordering, or different answer facts): model/library variance, not a code bug. Do not edit code. Re-run the scratch capture once more. If the second re-run matches, record the flake and continue. Otherwise STOP and report without further commits.
   This plan produces no captures, but the rule is stated here because every later plan in the phase inherits it.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: LangGraph dependencies + SQLite checkpoint artifact ignore</name>
  <files>examples/pyproject.toml, examples/uv.lock, .gitignore</files>
  <action>
**A. Dependencies:** `uv add --project examples langgraph langgraph-checkpoint-sqlite langsmith`.

Declare all three directly even though `langgraph` and `langsmith` are already present transitively — this project's convention is that any package an example imports directly is declared directly (same reasoning as `langchain-core`). `langgraph-checkpoint-sqlite` is the only genuinely new install; it pulls `aiosqlite` and `sqlite-vec`. Do not add `langgraph-checkpoint` (transitive dependency of `langgraph`). Do not change `requires-python`.

If resolution or installation fails on Python 3.14, stop and report; do not pin another Python and do not fall back to a different checkpointer package.

Record resolved versions for SUMMARY:
`uv run --project examples python -c "import importlib.metadata as m; [print(p, m.version(p)) for p in ('langgraph','langgraph-checkpoint','langgraph-checkpoint-sqlite','langsmith','langchain','langchain-core')]"`

**B. `.gitignore`:** append a new section after the mdBook section:
```
# LangGraph SQLite 체크포인터 데모가 만드는 임시 DB (WAL 사이드카 포함)
examples/ch04_langgraph/*.db
examples/ch04_langgraph/*.db-wal
examples/ch04_langgraph/*.db-shm
```
Three explicit lines rather than `*.db*`, so the intent is readable and nothing else is swallowed. The capture script in 04-03 also deletes these files at start and end; the ignore rule is the safety net, not the primary cleanup.

Commit: `chore(04-01): add langgraph, sqlite checkpointer and langsmith dependencies`. Stage `examples/pyproject.toml examples/uv.lock .gitignore`.
  </action>
  <verify>
- `/usr/bin/grep -cE '^\s+"(langgraph|langgraph-checkpoint-sqlite|langsmith)>=' examples/pyproject.toml` prints `3`, and `uv lock --project examples --check` exits 0.
- `uv run --project examples python -c "from langgraph.checkpoint.sqlite import SqliteSaver; print('SQLITE_OK')"` prints `SQLITE_OK`.
- `uv run --project examples python -c "from langgraph.graph import StateGraph, START, END, MessagesState; from langgraph.prebuilt import ToolNode, tools_condition; from langgraph.errors import GraphRecursionError; from langgraph.checkpoint.memory import InMemorySaver; print('IMPORTS_OK')"` prints `IMPORTS_OK`.
- Lazy-import invariant still holds: `uv run --project examples python -c "import sys, shared.config; assert 'torch' not in sys.modules; print('LAZY_OK')"` prints `LAZY_OK`.
- `/usr/bin/grep -c 'examples/ch04_langgraph/\*\.db' .gitignore` prints `3`.
  </verify>
  <done>`langgraph`, `langgraph-checkpoint-sqlite` and `langsmith` are directly declared and locked on Python 3.14, every LangGraph symbol this phase needs imports cleanly, and the SQLite demo's three artifact files are gitignored. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Gate scripts — enforce include-only mermaid fences, add path-scoped --exclude to the leak scanner</name>
  <files>scripts/check_book.py, scripts/check_leaks.py, scripts/test_check_leaks.py</files>
  <action>
**A. `scripts/check_book.py`** — one-line change plus its comment:
change `INCLUDE_ONLY_LANGS = ("python", "text", "ini")` to `INCLUDE_ONLY_LANGS = ("python", "text", "ini", "mermaid")`.

Why: today a `mermaid` fence is skipped entirely by `_check_code_blocks`, so a hand-typed diagram would slip past the "book shows only real, generated artifacts" rule. After this change a mermaid fence must be a single `{{#include ...}}` line, exactly like the python/text/ini fences. The existing `.out` rules apply unchanged and are already satisfied by this phase's design: 04-02 includes `outputs/ch04_langgraph/02_graph_mermaid.out` with the `:2:` suffix, which also satisfies `has_outputs_include`. Change nothing else in this file.

**B. `scripts/check_leaks.py`** — add a repeatable `--exclude GLOB`:
- add `import fnmatch` to the imports
- add the argument:
```python
    parser.add_argument("--exclude", action="append", default=[], metavar="GLOB",
                        help="Skip files whose path matches GLOB (repeatable). "
                             "Intended for vendored third-party assets, never for project content.")
```
- add a module-level helper:
```python
def _is_excluded(path: Path, patterns: list[str], cwd: Path) -> bool:
    """경로가 --exclude 글롭 중 하나와 맞으면 True. 벤더링된 외부 자산 전용."""
    if not patterns:
        return False
    candidates = {str(path), path.as_posix()}
    try:
        candidates.add(path.resolve().relative_to(cwd).as_posix())
    except (ValueError, OSError):
        pass
    return any(fnmatch.fnmatch(c, pat) for c in candidates for pat in patterns)
```
- in `main`, inside the per-file loop (the branch that walks `_iter_files(p)`), skip excluded files before reading them:
```python
            if _is_excluded(file_path, args.exclude, cwd):
                continue
```
Do not apply the exclusion to the `-` (stdin) branch — stdin has no path. Do not change `masking.py`, `collect_secrets`, `find_leaks`, or the `CLEAN` / findings output format.

Why this exists (put a short comment above the helper): `book/mermaid.min.js` is a 2.6 MB minified bundle vendored verbatim from a pinned upstream release. Literal-secret matching against a short secret produces coincidental hits inside such a bundle, so it is excluded by exact path and pinned by sha256 instead (Task 3). Project-authored files are never excluded.

**C. `scripts/test_check_leaks.py`** (new) — pytest tests that fail if `--exclude` is dropped or made too broad. Use `tmp_path`, `monkeypatch` and `capsys`, import the module as `check_leaks` (the `scripts/` dir is already on `sys.path` for these tests the same way `test_masking.py` does it; mirror that file's import style):
1. `test_finding_reported_without_exclude`: set `monkeypatch.setenv("LLM_API_KEY", "SUPERSECRET_TEST_VALUE_12345")`, write that string into `tmp_path/"vendor.min.js"`, call `check_leaks.main([str(tmp_path)])` → returns `1` and stdout does not contain `CLEAN`.
2. `test_excluded_file_is_skipped`: same setup, call `check_leaks.main([str(tmp_path), "--exclude", "*vendor.min.js"])` → returns `0` and stdout contains `CLEAN`.
3. `test_exclude_does_not_hide_other_files`: write the secret into both `vendor.min.js` and `chapter.md`, exclude only `*vendor.min.js` → returns `1`, and the printed finding mentions `chapter.md` and not `vendor.min.js`.
Assert on return codes and on `CLEAN`/filename presence only — never print or assert a secret value.

Commit: `feat(04-01): gate mermaid fences and add path-scoped --exclude to the leak scanner`. Stage `scripts/check_book.py scripts/check_leaks.py scripts/test_check_leaks.py`.
  </action>
  <verify>
- `uv run --project examples pytest -p no:cacheprovider -q scripts/test_masking.py scripts/test_check_leaks.py` passes (existing masking tests + 3 new).
- Existing book still passes with the stricter language list: `uv run --project examples python scripts/check_book.py` → 10× `PASS`, exit 0.
- Negative control (scratch only, never committed): create `$SCRATCH/bad.md` as a copy of `book/src/ch03_rag/01_load_split.md` with an extra hand-typed ```mermaid fence containing `graph TD;` and `A-->B;`; `uv run --project examples python scripts/check_book.py "$SCRATCH/bad.md"` prints `FAIL` mentioning the mermaid block. Delete the scratch file afterwards.
- `/usr/bin/grep -c '"mermaid"' scripts/check_book.py` prints `1`.
- `git diff HEAD~1 --stat -- scripts/masking.py` shows no change to `masking.py`.
  </verify>
  <done>A mermaid fence is now held to the same include-only standard as python/text/ini fences (proved by a failing negative control), and `check_leaks.py` can skip a named vendored asset without losing any other coverage (proved by three tests). All 10 existing chapters still pass. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Install and pin the mdbook-mermaid rendering pipeline (book.toml, vendored assets, CI step)</name>
  <files>book/book.toml, book/mermaid.min.js, book/mermaid-init.js, .github/workflows/deploy.yml</files>
  <action>
**A. Confirm the local binary, then install the assets:**
- `mdbook-mermaid --version` must print `mdbook-mermaid 0.17.0`. If it prints another version, stop and report rather than upgrading silently — the sha256 pins below belong to 0.17.0.
- Run `mdbook-mermaid install book` from the repo root. This writes `book/mermaid.min.js` and `book/mermaid-init.js` and edits `book/book.toml`.

**B. Review the `book/book.toml` diff.** Expected, and nothing else: `additional-js = ["mermaid.min.js", "mermaid-init.js"]` appended inside the existing `[output.html]` table, plus a new empty `[preprocessor]` table and `[preprocessor.mermaid]` with `command = "mdbook-mermaid"` at the end. `site-url`, `git-repository-url`, `edit-url-template`, `default-theme`, `preferred-dark-theme` and the whole `[output.html.search]` table must survive unchanged. If the tool reorders or drops any of those keys, restore them by hand and note it.

**C. Pin the assets.** Record and verify these sha256 values (measured during planning from mdbook-mermaid 0.17.0):
- `book/mermaid.min.js` → `eefea253bed9655e838eb874ff955c46872f982a8e26290c1dd2982ddc0a4703` (2,667,011 bytes)
- `book/mermaid-init.js` → `ccf746f10c0a71bd34799867d2f9860dddb5fc6efc6c50d1ea55a1eb1a7bd406` (1,262 bytes)
If either differs, stop and report — do not commit an unverified 2.6 MB third-party blob. Copy both values into the SUMMARY; 04-04's gate re-checks them.

**D. `.github/workflows/deploy.yml`:** add the pinned preprocessor install **before** the build step.
- In the `build` job's `env:` block, next to `MDBOOK_VERSION`, add:
  `MDBOOK_MERMAID_VERSION: "0.17.0" # book.toml의 [preprocessor.mermaid]가 이 바이너리를 호출한다`
- After the `Install mdBook (pinned)` step and before `Build book`, add a step mirroring the existing curl/tar pattern:
```yaml
      - name: Install mdbook-mermaid (pinned)
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/mdbook-mermaid"
          curl -sSfL "https://github.com/badboy/mdbook-mermaid/releases/download/v${MDBOOK_MERMAID_VERSION}/mdbook-mermaid-v${MDBOOK_MERMAID_VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar -xz -C "$RUNNER_TEMP/mdbook-mermaid"
          echo "$RUNNER_TEMP/mdbook-mermaid" >> "$GITHUB_PATH"
```
Do **not** run `mdbook-mermaid install` in CI — it would rewrite `book.toml` at build time; the assets and config are committed. Do not add python/uv/pip anywhere. Leave the `Build book` step's `ERROR` grep exactly as it is (the preprocessor's version-mismatch line is a `Warning`, not an `ERROR`, so it must not fail the build).

**E. Prove the pipeline end to end locally, without touching `book/src`.** In `$SCRATCH` only: copy `book/book.toml` into a throwaway book that has one chapter containing a ```mermaid fence whose body is a single `{{#include}}` of a two-line diagram file, copy the two asset files next to it, build it, and confirm the rendered HTML contains `<pre class="mermaid">` with the diagram text and a `src="mermaid-<hash>.min.js"` script tag. This proves the committed configuration works before any real chapter depends on it. (The real chapter's diagram arrives in 04-02.)

Commit: `build(04-01): render mermaid diagrams via pinned mdbook-mermaid preprocessor`. Stage `book/book.toml book/mermaid.min.js book/mermaid-init.js .github/workflows/deploy.yml`.
  </action>
  <verify>
- `mdbook-mermaid --version` → `mdbook-mermaid 0.17.0`.
- `shasum -a 256 book/mermaid.min.js book/mermaid-init.js` matches the two pinned values in C exactly.
- `/usr/bin/grep -c 'command = "mdbook-mermaid"' book/book.toml` → `1`; `/usr/bin/grep -c 'additional-js' book/book.toml` → `1`; `/usr/bin/grep -c 'limit-results' book/book.toml` → `1` (search config survived).
- Fresh build: `rm -rf book/book && mdbook build book 2>&1 | tee "$SCRATCH/build.log"`; `/usr/bin/grep -c ERROR "$SCRATCH/build.log"` → `0`; the only `Warning` line is the mdbook-mermaid 0.5.0-vs-0.5.3 version-mismatch line (`/usr/bin/grep -c 'built against version' "$SCRATCH/build.log"` → `1`, and no other `Warning`/`WARN` lines).
- The hashed assets reached the build output: `ls book/book/mermaid-*.min.js book/book/mermaid-init-*.js` lists exactly one of each, and the bundle is >1,000,000 bytes.
- Existing chapters unaffected: `uv run --project examples python scripts/check_book.py --html book/book` → 10× `PASS`, exit 0.
- Scratch end-to-end proof from E: rendered HTML contains `<pre class="mermaid">`, the diagram text, and a `src="mermaid-` script reference; it contains neither `{{#include` nor a literal ```mermaid fence.
- Leak scan with the new exclusion is CLEAN and the exclusion is actually needed (both facts recorded in SUMMARY):
  - `uv run --project examples python scripts/check_leaks.py book/mermaid.min.js` reports `literal-secret` findings (count only — this documents the false positive)
  - `uv run --project examples python scripts/check_leaks.py book/src examples outputs scripts .github --exclude 'book/mermaid.min.js'` → `CLEAN`
- CI invariant holds: `/usr/bin/grep -ciE 'setup-python|setup-uv|uv sync|uv run|pip install' .github/workflows/deploy.yml` → `0`. Workflow still parses: `python3 -c "import sys; sys.exit(0)"` is not enough — use `uv run --project examples python -c "import tomllib" ` only if needed; instead validate YAML with `uv run --project examples python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy.yml')); print('YAML_OK')"` if pyyaml is available, otherwise `gh workflow view deploy.yml --repo ohama/langchain-tutorial` is not applicable pre-push — in that case assert the file contains exactly one `mdbook-mermaid` install step and that `Install mdbook-mermaid` appears before `Build book` (line-number comparison with `/usr/bin/grep -n`).
- The pinned release asset exists: `curl -sSfI -o /dev/null -w '%{http_code}\n' -L "https://github.com/badboy/mdbook-mermaid/releases/download/v0.17.0/mdbook-mermaid-v0.17.0-x86_64-unknown-linux-gnu.tar.gz"` prints `200`.
  </verify>
  <done>`mdbook build book` turns a ```mermaid fence into a real `<pre class="mermaid">` diagram using a version-pinned, sha256-verified vendored bundle; the CI workflow installs the same pinned binary from its release tarball before building, using only curl/tar; all 10 existing chapters still pass and the fresh build has zero ERROR lines. Committed locally, not pushed.</done>
</task>

</tasks>

<verification>
Plan-level checks (toolchain only — no reader-visible content yet):
1. Every LangGraph/LangSmith symbol Phase 4 needs imports on Python 3.14, and the lock is consistent
2. A hand-typed mermaid diagram now FAILS the format gate, while an `{{#include}}`-only mermaid fence passes (negative control run)
3. `mdbook build` emits `<pre class="mermaid">` plus the hashed mermaid assets, with the single known benign preprocessor warning
4. The vendored bundle is sha256-pinned and excluded from secret scanning by exact path; `masking.py` is untouched and all other scanning is intact
5. CI installs the pinned preprocessor with curl/tar only, preserving the mdBook-only invariant
</verification>

<success_criteria>
- Dependencies, ignore rules, gate-script changes and the mermaid rendering pipeline are all committed locally on main
- No chapter, example or capture exists yet; nothing is pushed
- 04-02 can add a ```mermaid fence and have it both format-gated and rendered
</success_criteria>

<output>
After completion, create `.planning/phases/04-langgraph-langsmith/04-01-SUMMARY.md`. Record, with no secret values, absolute paths, email addresses or ports:
- resolved versions of langgraph / langgraph-checkpoint / langgraph-checkpoint-sqlite / langsmith, and whether `uv lock --check` passed
- the two vendored asset sha256 values and byte sizes (these are the pins 04-04 re-verifies)
- the exact count of `literal-secret` findings the un-excluded bundle produces, plus the measured secret length (a number only — never the value), as the documented justification for `--exclude`
- the `book/book.toml` diff shape (which keys were added, confirmation that search/site-url survived)
- the build log's ERROR count and the exact text of the single expected Warning line
- confirmation that `masking.py` was not modified
Before committing SUMMARY, check that `/usr/bin/grep -cE '/Users/[A-Za-z]|/private/|/tmp/claude' .planning/phases/04-langgraph-langsmith/04-01-SUMMARY.md` prints `0`.
</output>
