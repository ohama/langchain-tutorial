---
phase: 03-rag
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - examples/ch03_rag/03_rag_chain.py
  - outputs/ch03_rag/03_rag_chain.out
  - book/src/ch03_rag/03_rag_chain.md
  - book/src/SUMMARY.md
  - book/src/introduction.md
  - book/src/ch02_tools/02_tool_loop.md
autonomous: true
requirements: [RAG-04]

must_haves:
  truths:
    - "Chapter 'LCEL RAG 체인' shows, in real captured output, the chunks a retriever returned for a Korean question and the rendered prompt with that document text injected (RAG-04)"
    - "The chain {'context': retriever | format_docs, 'question': RunnablePassthrough()} | prompt | get_chat_model() | StrOutputParser() produces a real Korean answer stating the handbook fact (재택근무 주 2일), which the model could only know from the documents (RAG-04)"
    - "A question the documents do not cover (주차 요금) gets an answer saying it cannot be found in the documents, with the retrieved (irrelevant) sources printed"
    - "The phase reproducibility gate is applied: identical or wording-only re-captures pass; printed ids/timings/paths get fixed; structural divergence is re-run once and otherwise stops the plan before push"
    - "check_book.py passes for all 10 chapters, masking tests pass, the three leak scans are CLEAN, and embedding/chat/tool factories each exist in exactly one shared file"
    - "After push, the Actions run for HEAD succeeded, all 3 ch03 pages are live with their captured output, all 7 ch01/ch02 pages still serve theirs, the TOC lists 3부 RAG, and the intro and 2부 2장 link to 3부"
  artifacts:
    - path: "examples/ch03_rag/03_rag_chain.py"
      provides: "RAG-04 example: retriever + prompt injection + LCEL RAG chain"
      contains: "RunnablePassthrough"
    - path: "outputs/ch03_rag/03_rag_chain.out"
      provides: "Captured real output"
    - path: "book/src/ch03_rag/03_rag_chain.md"
      provides: "Chapter 3-3"
      contains: "{{#include ../../../outputs/ch03_rag/03_rag_chain.out:2:}}"
    - path: "book/src/SUMMARY.md"
      provides: "Complete 3부 with three chapter links"
      contains: "ch03_rag/03_rag_chain.md"
    - path: "book/src/introduction.md"
      provides: "Journey pointer to 3부"
      contains: "ch03_rag/01_load_split.md"
  key_links:
    - from: "examples/ch03_rag/03_rag_chain.py"
      to: "Chroma retriever"
      via: "vectorstore.as_retriever(search_kwargs={'k': 2}) piped into format_docs inside the chain dict"
      pattern: "retriever \\| format_docs"
    - from: "examples/ch03_rag/03_rag_chain.py"
      to: "examples/shared/config.py"
      via: "from shared.config import get_chat_model, get_embeddings"
      pattern: "from shared\\.config import get_chat_model, get_embeddings"
    - from: "git push origin main"
      to: "https://ohama.github.io/langchain-tutorial/ch03_rag/03_rag_chain.html"
      via: "deploy.yml Actions run for pushed HEAD"
      pattern: "deploy-pages"
---

<objective>
Finish part 3 (RAG). Chapter 3-3 injects retrieved chunks into a prompt and generates a Korean answer with an LCEL RAG chain, plus a not-in-the-documents question. Add journey pointers from the intro and 2부 2장. Then run the whole-phase release gate, mirroring 02-01 Task 3: reproducibility (with the structural-divergence stop rule), ch01/ch02 regression re-run, fresh build, format for all 10 chapters, masking tests, three leak scans, hygiene, invariants, push, Actions for HEAD, and live verification.

Purpose: RAG-04 completes the pipeline the phase goal promises (로딩 → 분할 → 임베딩 → 검색 → 생성). The gate proves the new heavy dependencies and `shared/config.py` change did not break the book or leak anything before it goes public.
Output: 1 example, 1 `.out`, 1 chapter; SUMMARY/intro/2부 2장 pointers; pushed main; verified live site.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rag/03-RESEARCH.md
@.planning/phases/03-rag/03-01-SUMMARY.md
@.planning/phases/02-tool-calling/02-01-PLAN.md
@examples/shared/config.py
@examples/ch03_rag/02_embed_search.py
@examples/data/ch03_rag/03_company_handbook.md
@scripts/run_examples.py
@book/src/SUMMARY.md
@book/src/introduction.md
@book/src/ch01_basics/04_lcel_runnables.md
@book/src/ch02_tools/02_tool_loop.md
@book/src/ch03_rag/02_embed_search.md

03-01-SUMMARY.md has the resolved versions, chunk counts, retrieval results and any query revisions from 03-01. Use `book/src/ch03_rag/02_embed_search.md` as the tone reference. 02-01-PLAN.md Task 3 is the gate being mirrored.

Facts already confirmed (no need to re-check):
- 03-01 committed everything locally but did NOT push. This plan's push publishes all of Phase 3 at once.
- `book/book.toml` has `create-missing = false`: add the SUMMARY link only after the chapter `.md` exists.
- The handbook states 재택근무 is allowed 주 2일 and never mentions parking. The chain questions below depend on this. Do not edit the documents, which are frozen.
- In LCEL, a dict in a pipe is coerced to `RunnableParallel` and a plain function to `RunnableLambda` (1부 4장 covers the operators).
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only (never `git add -A`/`git add .`). Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`. Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`; the orchestrator owns them. Never modify `examples/.env`.
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. After `test -n "$LITELLM_API_KEY"`, check each new or scratch `.out` with `/usr/bin/grep -cF -- "$LITELLM_API_KEY" FILE` → `0` before viewing it. Checks report counts only.
3. **grep:** the shell's `grep` is a ugrep wrapper. Use `/usr/bin/grep` or `git grep`.
4. **Outputs are generated, never hand-edited.** To change an output, change the `.py` and re-run the runner.
5. **Examples:** models come only from `from shared.config import get_chat_model, get_embeddings`. No `# ANCHOR` markers in example files, no `load_dotenv`, no sys.path hacks, no `@tool` literal, no `persist_directory`, one `Chroma.from_documents` call. Short `=== N. 섹션 ===` headers. Never print timings, absolute paths, the key, Chroma ids, or raw object reprs; use `type(x).__name__`. Sources are printed by file name from the repo-relative `source` metadata. Examples exit 0.
6. **Chapter format** (enforced by `scripts/check_book.py`): `##` headings exactly `개념: 왜 필요한가` / `최소 코드` / `실제 출력` / `요점 정리`, in that order. Every `python`/`text`/`ini` fence is a single `{{#include}}` line, `.out` includes use `:2:`, and `shared/*.py` is included only by anchor name. Only `bash` fences may hold typed content. Never write the literal word `ANCHOR` or `source-sha256` in prose.
7. **Truthful prose:** write 실제 출력 prose only after reading the captured `.out`. Describe model answers as "이번 캡처에서는 …"; do not claim the model always refuses or always answers identically.
8. **Retry rule:** if the capture fails to show its point (section 3's answer lacks the 주 2일 fact, or section 4's answer invents an answer instead of saying it is not in the documents), change ONLY the question text in `03_rag_chain.py` and re-capture. At most two revisions total. If it still fails, stop and report. Do not change the prompt template, `k`, documents, chunk parameters or `shared/config.py`.
9. **Reproducibility gate** (Task 2 step 1). Compare each scratch re-capture of `outputs/ch03_rag/*.out` with the committed file and classify it:
   - **Identical:** pass.
   - **Wording-only:** only model-written 답변 text differs, while the same sources in the same order, the same chunk counts and the same answer facts (주 2일 present; section 4 still says not found) remain. Pass. Keep the committed capture and record "wording-only".
   - **Printed volatile value** (id, timing, path, memory address): an example bug. Fix the print, re-capture into `outputs/`, update prose if the capture changed, and restart the gate from step 1.
   - **Structural divergence** (different retrieved documents or order, different chunk counts, start_index, score or embedding digits, or different answer facts): model/library variance, not a code bug. Do not edit code or re-capture. Re-run the scratch capture once more into a new scratch dir. If that re-run is identical or wording-only against the committed files, pass and record the flake. Otherwise STOP: do not push, and report which file/section diverged and how (no secret values or paths).
10. **Public-repo hygiene:** SUMMARY and any committed text must not contain absolute home paths, scratchpad paths, email addresses, hardware specs or backend ports. Write `~`, `<repo>`, `<scratchpad>`. Temp files and helper snippets go in `$SCRATCH` (the session scratchpad), never the repo. No `sleep`; use `gh run watch` and `curl --retry`. Run only one runner/embedding process at a time (bge-m3 shares memory with the LLM server).
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Chapter 3-3 (LCEL RAG 체인) + journey pointers</name>
  <files>examples/ch03_rag/03_rag_chain.py, outputs/ch03_rag/03_rag_chain.out, book/src/ch03_rag/03_rag_chain.md, book/src/SUMMARY.md, book/src/introduction.md, book/src/ch02_tools/02_tool_loop.md</files>
  <action>
**A. `examples/ch03_rag/03_rag_chain.py`** (RAG-04). Module docstring `"""3부 3장: 검색 결과를 프롬프트에 주입하는 LCEL RAG 체인으로 답을 만든다."""`.

Imports: `Path`, `Chroma`, `Document`, `StrOutputParser` (`langchain_core.output_parsers`), `ChatPromptTemplate` (`langchain_core.prompts`), `RunnablePassthrough` (`langchain_core.runnables`), `RecursiveCharacterTextSplitter`, and `get_chat_model, get_embeddings` from `shared.config`. If 03-01 applied its noise contingency (import order), apply the same order here.

Module level:
- `EXAMPLES_DIR`, `DATA_DIR`, `CHUNK_SIZE = 300`, `CHUNK_OVERLAP = 50` and `load_documents()` copied verbatim from `examples/ch03_rag/02_embed_search.py`
- `format_docs(docs) -> str` returning `"\n\n".join(d.page_content for d in docs)`
- `show_sources(docs)` printing `  - {Path(d.metadata["source"]).name} start_index={d.metadata["start_index"]}` per doc
- the prompt:
```python
PROMPT = ChatPromptTemplate.from_template(
    "다음 문서만 참고해서 질문에 한국어로 간단히 답하세요. "
    "문서에 답이 없으면 '문서에서 찾을 수 없습니다'라고 답하세요.\n\n"
    "문서:\n{context}\n\n질문: {question}"
)
```

Sections:
1. `=== 1. 색인과 retriever 준비 ===`
   - `chunks = RecursiveCharacterTextSplitter(chunk_size=CHUNK_SIZE, chunk_overlap=CHUNK_OVERLAP, add_start_index=True).split_documents(load_documents())`
   - `vectorstore = Chroma.from_documents(chunks, embedding=get_embeddings(), collection_name="ch03_rag")`
   - `retriever = vectorstore.as_retriever(search_kwargs={"k": 2})`
   - print `청크 {len(chunks)}개 색인, retriever 타입: {type(retriever).__name__}, k=2`
2. `=== 2. 검색 결과를 프롬프트에 주입하기 ===`
   - `question = "재택근무는 일주일에 며칠까지 할 수 있어?"`, `docs = retriever.invoke(question)`
   - print `질문: {question}`, `검색된 청크:`, then `show_sources(docs)`
   - print `--- 모델에게 보낼 프롬프트 ---` and `PROMPT.invoke({"context": format_docs(docs), "question": question}).to_string()`
3. `=== 3. LCEL RAG 체인 ===`
   - build the chain:
```python
chain = (
    {"context": retriever | format_docs, "question": RunnablePassthrough()}
    | PROMPT
    | get_chat_model()
    | StrOutputParser()
)
```
   - print `체인 타입: {type(chain).__name__}`
   - `answer = chain.invoke(question)`, then print `질문: {question}` and `답변: {answer}`
4. `=== 4. 문서에 없는 질문 ===`
   - `other = "회사 주차 요금은 한 달에 얼마야?"`
   - print `질문: {other}`, `검색된 청크:`, `show_sources(retriever.invoke(other))`
   - print `답변: {chain.invoke(other)}`

Capture: `uv run --project examples python scripts/run_examples.py ch03_rag/03_rag_chain.py` (with warm-up; this example calls the LLM).

**B. `book/src/SUMMARY.md`**: add `- [LCEL RAG 체인](ch03_rag/03_rag_chain.md)` as the third 3부 link.

**C. `book/src/ch03_rag/03_rag_chain.md`**, titled `# LCEL RAG 체인`:
- **개념: 왜 필요한가:**
  - Search alone returns chunk lists, not answers. RAG (Retrieval-Augmented Generation) puts the retrieved chunks into the prompt as context so the LLM answers from them. The model can answer about things it never learned, like this fictional handbook.
  - An instruction to say "문서에서 찾을 수 없습니다" when the answer is absent reduces ungrounded answers, but does not guarantee them away.
  - Tie to 1부 4장: the whole flow is one LCEL pipe. A dict becomes a `RunnableParallel`, `retriever | format_docs` turns documents into one string, and `RunnablePassthrough` forwards the question unchanged.
- **최소 코드:**
  - `### 예제 코드`: path, `python` include, bullets on:
    - the retriever (`as_retriever`, `k`)
    - `format_docs`
    - the prompt's grounding instruction
    - the chain dict and why the same question string reaches both branches
    - section 2 performing by hand what the chain does internally
    Then the `bash` run command `uv run python ch03_rag/03_rag_chain.py`.
- **실제 출력:** the standard LLM capture sentence used in 2부 (actual run with local `flashnext`, may differ character-by-character), then the `text` include with `:2:`. Interpret from the actual `.out`:
  - which chunks were retrieved for the 재택근무 question, and that the rendered prompt contains their text
  - the chain type
  - the answer and which document sentence it came from
  - for the parking question: that the retriever still returned the nearest (irrelevant) chunks, since it always returns k results, and what the model answered
- **요점 정리:** 3–5 bullets. End with a plain-text (no link) sentence that the next part rebuilds the 2부 tool loop as a LangGraph graph.

**D. Journey pointers (prose only):**
- `book/src/introduction.md`: in `## 앞으로의 여정`, change the plain `RAG` into `[RAG](ch03_rag/01_load_split.md)`. Leave the other stages unchanged.
- `book/src/ch02_tools/02_tool_loop.md`: append one final bullet to `## 요점 정리`: `- 바로 다음 [3부 RAG](../ch03_rag/01_load_split.md)에서는 잠시 도구 루프를 떠나, 검색한 문서를 모델에게 건네 답을 만드는 방법을 다룬다.` Change nothing else.

Commit: `feat(03-02): chapter 3-3 (LCEL RAG 체인) + 3부 pointers`.
  </action>
  <verify>
- The runner prints `OK`. The key count on `outputs/ch03_rag/03_rag_chain.out` is `0`. No `Traceback`, `it/s]`, `Loading weights` or `0x` in it.
- The `.out` contains:
  - `VectorStoreRetriever` and `RunnableSequence`
  - in section 2, `03_company_handbook.md` among the retrieved chunks, the rendered prompt containing `질문: 재택근무` and the handbook's 재택근무 sentence
  - in section 3, a `답변:` line containing `2일` or `2회` or `이틀`
  - in section 4, a `답변:` line containing `찾을 수 없`
  Otherwise apply rule 8.
- `git grep -n "HuggingFaceEmbeddings(" -- examples ':!examples/.venv'` matches only `examples/shared/config.py`. `git grep -n "ChatOpenAI(" -- examples` matches only `examples/shared/config.py`.
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`.
- `uv run --project examples python scripts/check_book.py --html book/book` → 10× `PASS`.
  </verify>
  <done>Chapter 3-3 shows real retrieval, the injected prompt, an LCEL RAG chain answering the handbook question from the documents, and a grounded "not found" answer. 3부 is complete in SUMMARY. The intro and 2부 2장 point to 3부. 10 chapters pass. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-phase gate: reproducibility, regression, format, masking, leak scan, hygiene, invariants, push, Actions + live verification</name>
  <files>(verification only; fixes go to whichever file a check implicates: examples/ch03_rag/*.py with re-captured outputs/ch03_rag/*.out, or book/src/**/*.md)</files>
  <action>
Run each gate in order. Fix failures at their source (re-capture via the runner, never hand-edit) and re-run until all pass, except where rule 9 says STOP.

1. **Reproducibility + regression (one run):**
   - `uv run --project examples python scripts/run_examples.py --outputs-dir "$SCRATCH/rerun"` (no targets = all 10 examples, one warm-up). Require 10× `OK`.
   - Key-count all 10 scratch files (each `0`).
   - ch03: `diff outputs/ch03_rag/<name>.out "$SCRATCH/rerun/ch03_rag/<name>.out"` for all three, classified per rule 9. `01_load_split` and `02_embed_search` are LLM-free, so any difference there is volatile or structural, never wording-only. On structural divergence, re-run only `ch03_rag` into `$SCRATCH/rerun2` once, then pass or STOP per rule 9.
   - ch01/ch02 regression: `shared/config.py` changed additively in 03-01, and these examples and the config code they exercise are byte-identical. The gate requires all 7 `OK`, no `Traceback` in the scratch files, and each committed `outputs/ch0[12]_*/*.out` first line still equal to `# source-sha256: ` + `shasum -a 256` of its `.py`. Diff them against the committed files and record identical/wording-only/other in SUMMARY as information only. Do not re-capture committed ch01/ch02 outputs.
2. **Fresh build:** `rm -rf book/book && mdbook build book 2>&1 | tee "$SCRATCH/final_build.log"`. Require exit 0 and zero `ERROR`/`WARN` lines.
3. **Format / include-only:** `uv run --project examples python scripts/check_book.py --html book/book` → 10× `PASS` (5 ch01_basics + 2 ch02_tools + 3 ch03_rag), exit 0.
4. **Masking tests:** `uv run --project examples pytest -p no:cacheprovider -q scripts/test_masking.py` passes.
5. **Leak scan:**
   - `uv run --project examples python scripts/check_leaks.py outputs book/src examples book/book .planning/phases/03-rag` → `CLEAN`
   - `git ls-files -z | xargs -0 uv run --project examples python scripts/check_leaks.py --secrets-only` → `CLEAN`
   - `git log -p --all | uv run --project examples python scripts/check_leaks.py --secrets-only -` → `CLEAN`
6. **Git hygiene:**
   - `git ls-files | /usr/bin/grep -E '(^|/)\.env$|(^|/)\.venv/|^\.claude/|^book/book/|__pycache__|\.pytest_cache'` prints nothing
   - `test ! -e .venv && test ! -e .pytest_cache && test ! -e main.py`
   - `git ls-files outputs | wc -l` = 10 and `git ls-files examples/data/ch03_rag | wc -l` = 3
   - every `outputs/ch03_rag/*.out` first line equals `# source-sha256: ` + `shasum -a 256` of the matching `examples/ch03_rag/*.py`
   - `git status --porcelain --ignored examples outputs | /usr/bin/grep -iE 'chroma|sqlite|\.parquet'` prints nothing (ephemeral Chroma left no artifacts)
   - `/usr/bin/grep -c '^LLM_API_KEY=$' examples/.env.example` = 1 (no real value), and `uv lock --project examples --check` exits 0
   - `/usr/bin/grep -ciE 'setup-python|setup-uv|uv sync|uv run|pip install' .github/workflows/deploy.yml` = 0 (CI stays mdBook-only)
   - `git status --porcelain` shows no untracked files except orchestrator-owned `.planning/` docs, which stay unstaged
7. **Invariants:**
   - `git grep -n "ChatOpenAI(" -- examples` → only `examples/shared/config.py`
   - `git grep -n "HuggingFaceEmbeddings(" -- examples ':!examples/.venv'` → only `examples/shared/config.py`
   - `git grep -n "@tool" -- examples ':!examples/.venv'` → only `examples/shared/tools.py`
   - `git grep -n "langchain_community" -- examples ':!examples/.venv'` → nothing
   - `git grep -nE "persist_directory\s*=" -- examples ':!examples/.venv'` → nothing
   - `uv run --project examples python -c "import sys, shared.config; assert 'torch' not in sys.modules; print('LAZY_OK')"` → `LAZY_OK`
   - also record, without failing, `/usr/bin/grep -c 'name = "langchain-community"' examples/uv.lock`; if nonzero, record which package pulls it
8. **Push:** only if steps 1–7 passed (never after a rule-9 STOP).
   - Commit any fixes, then `git push origin main`.
   - Get the run with `gh run list --repo ohama/langchain-tutorial --workflow deploy.yml --branch main --limit 1 --json databaseId,headSha --jq '.[0]'`. Repeat the command (no sleep) until `headSha` equals `git rev-parse HEAD`.
   - Then `gh run watch "$RUN_ID" --repo ohama/langchain-tutorial --exit-status`.
9. **Live verification** with a short Python snippet in `$SCRATCH` (not a repo file), fetching pages under `https://ohama.github.io/langchain-tutorial/` with `curl -sSf --retry 12 --retry-delay 10 --retry-all-errors`:
   - For each of the 10 chapter pages (`ch03_rag/01_load_split.html`, `ch03_rag/02_embed_search.html`, `ch03_rag/03_rag_chain.html`, the 2 ch02_tools pages, the 5 ch01_basics pages), assert the `html.unescape`d page contains a distinctive stripped line from line 2+ of the matching `.out`, and contains neither `{{#include` nor `이 장은 작성 중입니다`. Distinctive lines: for ch03, use `문서 3개 -> 청크` (01), `차원: 1024` (02) and the section-3 `답변:` line (03). Print `LIVE_OK <name>`.
   - `toc.html` contains `3부 RAG`, `2부 도구 호출`, `1부 기초` and `부록`.
   - `introduction.html` and `ch02_tools/02_tool_loop.html` both contain `ch03_rag/01_load_split.html`.
   - `ch01_basics/01_chat_model.html` contains `EMBEDDING_MODEL`.
   - A nonexistent path returns 404.
  </action>
  <verify>
- The ch03 re-run is identical or wording-only, or passed on its single structural re-run. The ch01/ch02 regression re-run is 7× OK with hashes matching.
- Fresh build: 0 ERROR/WARN. `check_book.py --html book/book` → 10× PASS, exit 0. Masking tests pass.
- All three `check_leaks` invocations print `CLEAN`. All hygiene and invariant checks pass.
- `gh run view "$RUN_ID" --repo ohama/langchain-tutorial --json conclusion,headSha --jq '.conclusion'` prints `success`, and headSha matches local HEAD.
- The live snippet prints `LIVE_OK` for all 10 chapter pages, and the toc, pointer, 1부 1장 env-line and 404 checks pass.
  </verify>
  <done>Phase 3's three RAG chapters and all earlier chapters pass the format gate. Captures are reproducible under the classification rule and leak-free, including tracked files and history. Factories and CI invariants hold. The final commit is pushed, the Actions run for that SHA succeeded, and the live site serves 3부 with real captures, an updated TOC and working pointers from the intro and 2부 2장.</done>
</task>

</tasks>

<verification>
Phase 3 success criteria, end to end:
1. Chapter 3-1 (live) shows Korean markdown loaded as `Document` and split by `RecursiveCharacterTextSplitter` with real chunk count and contents (RAG-01)
2. Chapter 3-2 (live) shows bge-m3 local embeddings on MPS with dimension 1024 (RAG-02)
3. Chapter 3-2 (live) shows Chroma search results for Korean queries (RAG-03)
4. Chapter 3-3 (live) shows the retrieved context injected into the prompt and an LCEL RAG chain generating a real Korean answer (RAG-04)
5. Pipeline invariants: 10× PASS, single chat/embedding/tool factories, CLEAN leak scans, CI mdBook-only, successful deploy for HEAD
</verification>

<success_criteria>
- RAG-04 chapter published with a real captured RAG answer, completing RAG-01..04 live
- The whole-phase gate (reproducibility with explicit structural-divergence stop, regression, format, masking, leaks, hygiene, invariants) passed before push
- The deployed site for the pushed SHA is verified live, with no regression in 1부/2부
</success_criteria>

<output>
After completion (or after a rule-8/rule-9 STOP, marking status accordingly), create `.planning/phases/03-rag/03-02-SUMMARY.md`. Record, with no secret values, absolute paths, email addresses or ports:
- the exact questions used (and any rule-8 revisions, old → new), the retrieved sources for each, and both final answers verbatim
- the reproducibility classification per ch03 file (identical / wording-only / flake re-run / STOP with description), and the ch01/ch02 regression diff classification (information only)
- the gate results list (build, 10× PASS, masking, 3 leak scans, hygiene, invariants, langchain-community lock count)
- the final run ID, the pushed SHA and the live URLs checked
Before committing SUMMARY, check that `/usr/bin/grep -cE '/Users/[A-Za-z]|/private/|/tmp/claude' .planning/phases/03-rag/03-02-SUMMARY.md` prints `0`.
</output>
