---
phase: 03-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/pyproject.toml
  - examples/uv.lock
  - examples/shared/config.py
  - examples/.env.example
  - examples/data/ch03_rag/01_langchain_intro.md
  - examples/data/ch03_rag/02_rag_guide.md
  - examples/data/ch03_rag/03_company_handbook.md
  - examples/ch03_rag/01_load_split.py
  - outputs/ch03_rag/01_load_split.out
  - book/src/ch03_rag/01_load_split.md
  - examples/ch03_rag/02_embed_search.py
  - outputs/ch03_rag/02_embed_search.out
  - book/src/ch03_rag/02_embed_search.md
  - book/src/SUMMARY.md
  - book/src/ch01_basics/01_chat_model.md
autonomous: true
requirements: [RAG-01, RAG-02, RAG-03]

must_haves:
  truths:
    - "Chapter '문서 로딩과 분할' shows, in real captured output, 3 original Korean markdown documents loaded as Document objects with repo-relative `source` metadata, and RecursiveCharacterTextSplitter(chunk_size=300, chunk_overlap=50, add_start_index=True) producing a printed chunk count plus per-chunk source/start_index/length/preview (RAG-01)"
    - "Chapter '임베딩과 벡터스토어 검색' shows a real BAAI/bge-m3 run on device mps: dimension 1024, first values to 6 decimals, L2 norm 1.0000, and a similar-meaning sentence pair scoring higher than an unrelated pair (RAG-02)"
    - "The same chapter indexes every chunk into an ephemeral Chroma collection (no persist directory) and shows 3 Korean queries whose rank-1 result comes from the intended sample document, with distances to 4 decimals (RAG-03)"
    - "The embedding model is constructed only in examples/shared/config.py get_embeddings(): importing shared.config does not import torch, the noise-silencing env vars are set before the HuggingFace import, and captured .out files contain no progress bars"
    - "Captured ch03 outputs contain no ids, timings, memory addresses or absolute paths, and a scratch re-capture of both (LLM-free) examples is byte-identical"
    - "All 9 chapters pass check_book.py, mdbook build has zero ERROR, and 1부 1장 explains the new optional EMBEDDING_* lines it now renders from .env.example"
  artifacts:
    - path: "examples/shared/config.py"
      provides: "get_embeddings() single embedding factory (lazy import, env-before-import, device auto with cpu fallback, normalize_embeddings)"
      contains: "ANCHOR: embeddings"
    - path: "examples/data/ch03_rag/03_company_handbook.md"
      provides: "Original fictional handbook with fixed facts used by retrieval (03-01) and the RAG chain (03-02)"
      contains: "재택근무"
    - path: "examples/ch03_rag/01_load_split.py"
      provides: "RAG-01 example: pathlib -> Document -> RecursiveCharacterTextSplitter"
      contains: "RecursiveCharacterTextSplitter"
    - path: "examples/ch03_rag/02_embed_search.py"
      provides: "RAG-02/RAG-03 example: embeddings + Chroma.from_documents + similarity_search_with_score"
      contains: "similarity_search_with_score"
    - path: "outputs/ch03_rag/01_load_split.out"
      provides: "Captured real output"
    - path: "outputs/ch03_rag/02_embed_search.out"
      provides: "Captured real output"
    - path: "book/src/ch03_rag/01_load_split.md"
      provides: "Chapter 3-1"
      contains: "{{#include ../../../outputs/ch03_rag/01_load_split.out:2:}}"
    - path: "book/src/ch03_rag/02_embed_search.md"
      provides: "Chapter 3-2"
      contains: "{{#include ../../../examples/shared/config.py:embeddings}}"
    - path: "book/src/SUMMARY.md"
      provides: "New part between 2부 and 부록"
      contains: "# 3부 RAG"
  key_links:
    - from: "examples/ch03_rag/02_embed_search.py"
      to: "examples/shared/config.py get_embeddings"
      via: "from shared.config import get_embeddings (no HuggingFaceEmbeddings( in chapter examples)"
      pattern: "from shared\\.config import .*get_embeddings"
    - from: "examples/shared/config.py get_embeddings"
      to: "langchain_huggingface.HuggingFaceEmbeddings"
      via: "function-local import placed after os.environ.setdefault(...) lines"
      pattern: "^\\s+from langchain_huggingface import HuggingFaceEmbeddings"
    - from: "examples/ch03_rag/02_embed_search.py"
      to: "langchain_chroma.Chroma (ephemeral)"
      via: "Chroma.from_documents(chunks, embedding=..., collection_name=...) without persist_directory"
      pattern: "Chroma\\.from_documents\\("
    - from: "examples/ch03_rag/*.py"
      to: "examples/data/ch03_rag/*.md"
      via: "DATA_DIR = EXAMPLES_DIR / 'data' / 'ch03_rag'; metadata source = repo-relative POSIX path"
      pattern: "data.*ch03_rag"
---

<objective>
Start part 3 (RAG) of the book. Add the RAG dependencies and a single lazy `get_embeddings()` factory (BAAI/bge-m3) to `shared/config.py`. Write three original Korean sample documents. Then author chapter 3-1 (load markdown as `Document`, split with `RecursiveCharacterTextSplitter`) and chapter 3-2 (embed with bge-m3 on MPS, index into ephemeral Chroma, search with Korean queries), each with real captures.

Purpose: RAG-01..03 are the retrieval half of the pipeline. Plan 03-02 builds the LCEL RAG chain (RAG-04) on these exact documents, chunk parameters and embeddings, then runs the whole-phase release gate. This plan commits locally only. It does NOT push; 03-02 pushes after the full gate.
Output: new deps + lock, `get_embeddings()`, `.env.example` section, 3 sample docs, 2 examples, 2 `.out`, 2 chapters, 3부 in SUMMARY, 1부 1장 note.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rag/03-RESEARCH.md
@.planning/phases/02-tool-calling/02-01-SUMMARY.md
@examples/pyproject.toml
@examples/shared/config.py
@examples/.env.example
@scripts/run_examples.py
@scripts/check_book.py
@book/src/SUMMARY.md
@book/src/ch01_basics/01_chat_model.md
@book/src/ch02_tools/02_tool_loop.md

03-RESEARCH.md is the primary input. Its stack, Patterns 1–5 and Pitfalls 1–4 were verified live on Python 3.14 against this project's endpoint. This plan corrects one gap in its Pattern 1 snippet: `get_embeddings()` must call `load_dotenv(ENV_PATH, override=False)` itself, otherwise `EMBEDDING_*` values in `examples/.env` would be ignored. Use `book/src/ch02_tools/02_tool_loop.md` as the tone/structure reference (평서체 "~다").

Facts already confirmed (no need to re-check):
- `examples/` is installed editable, so `shared.*` imports from scratch scripts and examples without sys.path hacks.
- `run_examples.py` globs `examples/ch*` for numbered `.py` files and runs each as its own subprocess with cwd `examples/`. `examples/data/` is never executed. `check_book.py` discovers chapters from `SUMMARY.md` links. Neither script needs changes.
- `book/book.toml` has `create-missing = false`: add a SUMMARY link only once its `.md` exists, or `mdbook build` fails.
- `book/src/ch01_basics/01_chat_model.md` includes the WHOLE `examples/.env.example` and says there are exactly three values to change. New `.env.example` lines therefore appear in 1부 1장; Task 3 adds one explanatory sentence there.
- The deploy workflow only runs `mdbook build` (no Python/uv), so new Python deps do not affect CI.
- The scanner regex for home paths needs an alphanumeric character right after the users/home prefix, so literal patterns in docs are safe, but real paths are not.
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only (never `git add -A`/`git add .`). Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`. Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`; the orchestrator owns them. Never modify `examples/.env`. Do NOT push in this plan.
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. After `test -n "$LITELLM_API_KEY"`, check each new `.out` with `/usr/bin/grep -cF -- "$LITELLM_API_KEY" FILE` → `0` before viewing it. Checks report counts only.
3. **grep:** the shell's `grep` is a ugrep wrapper. Use `/usr/bin/grep` or `git grep`.
4. **Outputs are generated, never hand-edited.** To change an output, change the `.py` and re-run the runner. The `# source-sha256:` header hashes only the example file, so if `shared/config.py` or any `examples/data/ch03_rag/*.md` changes after a capture, re-capture every ch03 example captured so far.
5. **Examples:** the embedding model comes only from `from shared.config import get_embeddings` (no `HuggingFaceEmbeddings(` outside `shared/config.py`). No `# ANCHOR` markers in example files, no `load_dotenv`, no sys.path hacks, no `@tool` literal. Short `=== N. 섹션 ===` headers. Print only reproducible values: embedding components as `f"{x:.6f}"`, scores/similarities/norms as `f"{v:.4f}"`. Never print timings, absolute paths, the key, Chroma ids, or raw object reprs (memory addresses); use `type(x).__name__` or specific attributes instead. `metadata["source"]` is the repo-relative POSIX path (e.g. `examples/data/ch03_rag/01_langchain_intro.md`), never an absolute path. Chroma is ephemeral: no `persist_directory`, and call `Chroma.from_documents` exactly once per script. Examples exit 0.
6. **Chapter format** (enforced by `scripts/check_book.py`): `##` headings exactly `개념: 왜 필요한가` / `최소 코드` / `실제 출력` / `요점 정리`, in that order (`###` allowed inside). Every `python`/`text`/`ini` fence is a single `{{#include}}` line. `.out` includes use `:2:`. `shared/config.py` is included only by anchor name. Only `bash` fences may hold typed content, and only reader commands. Do not write the literal word `ANCHOR` or `source-sha256` in prose. Include paths from `book/src/ch03_rag/` are `../../../examples/...` and `../../../outputs/...`.
7. **Truthful prose:** write each chapter's 실제 출력 prose only after reading its captured `.out`. Describe numbers, counts and rankings as "이번 캡처에서는 …" and assert library behavior only where you verified it (see the scratch checks in the tasks).
8. **Retry rule (captures):** if a capture fails to show its point (a query's rank-1 result is not the intended document, or the similar pair does not beat the unrelated pair), change ONLY the query/sentence text in that example `.py` and re-capture. At most two such revisions per example file. If it still fails, stop and report. Do not change sample documents, chunk parameters, `k`, or `shared/config.py` to force a result. The sample documents are frozen once Task 2's capture is committed.
9. **Reproducibility rule:** a scratch re-capture that differs from the committed `.out` gets classified. Both examples in this plan are LLM-free, so no wording-only case exists.
   - **Printed volatile value** (id, timing, path, memory address): an example bug. Fix the print, re-capture, re-check.
   - **Structural difference** (chunk count, chunk start_index/length, retrieved documents or their order, score or embedding digits): model/library variance, not a code bug. Do not edit code. Re-run the scratch capture once more. If the second re-run matches the committed output, record the flake in SUMMARY and continue. Otherwise stop and report without further commits.
10. **Memory:** bge-m3 shares unified memory with the local LLM server. Never run two runner or embedding processes at the same time.
11. **Public-repo hygiene:** SUMMARY and any committed text must not contain absolute home paths, scratchpad paths, email addresses, hardware specs or backend ports. Write `~`, `<repo>`, `<scratchpad>`. Temp files go in the session scratchpad (`$SCRATCH`), never the repo. No `sleep`.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: RAG dependencies, get_embeddings() factory, .env.example section, original Korean sample documents</name>
  <files>examples/pyproject.toml, examples/uv.lock, examples/shared/config.py, examples/.env.example, examples/data/ch03_rag/01_langchain_intro.md, examples/data/ch03_rag/02_rag_guide.md, examples/data/ch03_rag/03_company_handbook.md</files>
  <action>
**A. Dependencies:** `uv add --project examples langchain-text-splitters langchain-chroma langchain-huggingface sentence-transformers`. `torch` and `chromadb` come in transitively; do not add them explicitly. Do not change `requires-python`. If resolution or installation fails on Python 3.14, stop and report; do not pin another Python. Record the resolved versions for SUMMARY with:
`uv run --project examples python -c "import importlib.metadata as m; [print(p, m.version(p)) for p in ('langchain-core','langchain-text-splitters','langchain-chroma','langchain-huggingface','sentence-transformers','torch','chromadb')]"`

**B. `examples/shared/config.py`** (additive change only):
- Change line 1's docstring to `"""예제 전체가 공유하는 모델 설정. 모든 예제는 여기서만 채팅 모델과 임베딩 모델을 만든다."""`.
- Append, after `# ANCHOR_END: structured`, a blank line and exactly this block. No new top-level imports: `os`, `load_dotenv` and `ENV_PATH` already exist.
```python
# ANCHOR: embeddings
def get_embeddings(**overrides):
    load_dotenv(ENV_PATH, override=False)  # EMBEDDING_* 값도 .env에서 읽는다
    # 진행 표시줄·로그가 출력에 섞이지 않도록 라이브러리를 import하기 "전에" 설정한다
    os.environ.setdefault("HF_HUB_DISABLE_PROGRESS_BARS", "1")
    os.environ.setdefault("TRANSFORMERS_VERBOSITY", "error")
    os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
    os.environ.setdefault("ANONYMIZED_TELEMETRY", "False")  # 함께 쓰는 Chroma의 익명 통계 전송 끄기

    # 무거운 라이브러리(torch 등)는 임베딩이 필요할 때만 불러온다 — 1·2부 예제는 영향 없음
    import torch
    from langchain_huggingface import HuggingFaceEmbeddings

    model_name = os.environ.get("EMBEDDING_MODEL", "").strip() or "BAAI/bge-m3"
    device = os.environ.get("EMBEDDING_DEVICE", "").strip()
    if not device:  # 비워 두면 mps를 쓸 수 있으면 mps, 아니면 cpu
        device = "mps" if torch.backends.mps.is_available() else "cpu"

    params = dict(
        model_name=model_name,
        model_kwargs={"device": device},
        encode_kwargs={"normalize_embeddings": True},  # 길이 1로 정규화 → 내적 = 코사인 유사도
    )
    params.update(overrides)
    return HuggingFaceEmbeddings(**params)
# ANCHOR_END: embeddings
```
Do not touch the existing `settings`/`chat_model`/`structured` regions. ch01/ch02 outputs are NOT re-captured, because their exercised code is byte-identical. The final gate in 03-02 re-runs them as a regression check.

**C. `examples/.env.example`:** append (keep the existing 8 lines unchanged):
```

# 임베딩 모델 설정 (3부 RAG에서 사용, 선택)
# EMBEDDING_DEVICE를 비워 두면 mps를 쓸 수 있으면 mps, 아니면 cpu를 자동으로 고릅니다. (cpu, mps, cuda 등 직접 지정 가능)
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=
```

**D. Sample documents** in `examples/data/ch03_rag/`. They are original Korean prose written by you: no copied text, formal style (합니다체). Each document:
- starts with one `# 제목`, has 3–5 `## 소제목` sections, and separates paragraphs with blank lines
- is 1000–1500 characters long (Python `len` of the file text)
- covers a topic that does NOT overlap the other two documents
- contains none of: `@`, `@tool`, `ChatOpenAI(`, `HuggingFaceEmbeddings(`, `bge-m3`, `persist_directory`, `ANCHOR`, `source-sha256`, `{{`, triple backticks, `/Users/`, `/home/`, `sk-`, `Bearer`, `http://`, `https://`, `주차`, any specific model name, or real people/companies (other than naming LangChain itself)

Documents:
- `01_langchain_intro.md` (LangChain 핵심 개념 노트). Cover:
  - what LangChain is
  - the Runnable interface (`invoke`/`batch`/`stream`)
  - LCEL's pipe operator `|`: connects the previous component's output to the next component's input
  - prompt templates, output parsers, and the idea that the model requests tools while code runs them
  Keep it accurate for LangChain 1.x. Do NOT discuss chunking, embeddings or RAG.
- `02_rag_guide.md` (RAG 개념 안내). Cover:
  - RAG (검색 증강 생성) and why it is needed (information the model never learned, e.g. internal/recent docs; fewer ungrounded answers)
  - the stages 로딩 → 분할 → 임베딩 → 저장 → 검색 → 생성
  - chunk size trade-off
  - why chunks overlap: context cut at a boundary is kept in the neighboring chunk
  - embeddings: similar meaning → nearby vectors
  Do NOT discuss LCEL or company policies.
- `03_company_handbook.md` (가상 회사 근무 규정). The first paragraph states the document is fiction invented for this book's examples and unrelated to any real company or organization. It must contain these facts, which later plans depend on:
  - 연차 휴가: 입사 첫해에는 한 달 개근할 때마다 1일씩(최대 11일) 생기고, 2년 차부터는 매년 15일. 사용 3일 전까지 신청.
  - 재택근무: 주 2일까지 가능. 전날 오후 6시까지 팀 채널에 신청. 수요일은 팀 회의가 있어 모두 사무실에 출근.
  - 교육 지원: 연 100만 원 한도, 도서 구입과 온라인 강의 포함.
  - 업무 장비: 노트북은 3년마다 교체.
  It must NOT mention parking, meals, gyms or salary amounts; 03-02 asks about parking as its "not in the documents" question.

Commit: `feat(03-01): RAG dependencies, get_embeddings factory and Korean sample documents`. Stage `examples/pyproject.toml examples/uv.lock examples/shared/config.py examples/.env.example examples/data/ch03_rag/*.md`.
  </action>
  <verify>
- `/usr/bin/grep -cE '^\s+"(langchain-text-splitters|langchain-chroma|langchain-huggingface|sentence-transformers)>=' examples/pyproject.toml` prints `4`, and `uv lock --project examples --check` exits 0.
- `git diff --numstat HEAD~1 -- examples/shared/config.py` (after commit) shows exactly `1` deletion (the docstring). `/usr/bin/grep -c "ANCHOR: " examples/shared/config.py` prints `4`.
- Lazy import: `uv run --project examples python -c "import sys, shared.config; assert 'torch' not in sys.modules and 'langchain_huggingface' not in sys.modules; print('LAZY_OK')"` prints `LAZY_OK`.
- Smoke test via a scratch script `$SCRATCH/embed_smoke.py` containing `from shared.config import get_embeddings; e = get_embeddings(); v = e.embed_query("테스트 문장입니다."); print(len(v), e.model_kwargs)`. Run `uv run --project examples python "$SCRATCH/embed_smoke.py" > "$SCRATCH/smoke.out" 2> "$SCRATCH/smoke.err"`:
  - `$SCRATCH/smoke.out` is exactly one line `1024 {'device': 'mps'}`
  - `/usr/bin/grep -cE "it/s\]|Loading weights|%\|" "$SCRATCH/smoke.out"` prints `0`
  - record the line count of `smoke.err` (target 0) for SUMMARY
  If the device is not `mps`, stop and report (environment issue).
- Sample-doc check (scratch heredoc run with `uv run --project examples python -`): for each `examples/data/ch03_rag/*.md`, print name, `len(text)`, and `OK` only if 1000 <= len <= 1500 and none of the forbidden substrings from D occur. Use `chr(96)*3` for the backtick fence. All three print `OK`. `/usr/bin/grep -c "재택근무" examples/data/ch03_rag/03_company_handbook.md` ≥ 1.
- `git grep -n "HuggingFaceEmbeddings(" -- examples ':!examples/.venv'` matches only `examples/shared/config.py`.
  </verify>
  <done>The four RAG deps are locked on Python 3.14. `get_embeddings()` exists once, as a lazy, quiet, device-auto factory returning 1024-dim normalized bge-m3 embeddings on mps. `.env.example` documents the optional `EMBEDDING_*` settings. Three original, non-overlapping Korean documents with the fixed handbook facts exist. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Chapter 3-1 (문서 로딩과 분할) + 3부 in SUMMARY</name>
  <files>examples/ch03_rag/01_load_split.py, outputs/ch03_rag/01_load_split.out, book/src/ch03_rag/01_load_split.md, book/src/SUMMARY.md</files>
  <action>
**A. `examples/ch03_rag/01_load_split.py`** (RAG-01, no LLM, no embeddings). Module docstring `"""3부 1장: 한국어 마크다운 문서를 Document로 읽고 RecursiveCharacterTextSplitter로 나눈다."""`.

Imports: `Path`, `Document` from `langchain_core.documents`, `RecursiveCharacterTextSplitter` from `langchain_text_splitters`.

Module level:
- `EXAMPLES_DIR = Path(__file__).resolve().parent.parent`
- `DATA_DIR = EXAMPLES_DIR / "data" / "ch03_rag"`
- `CHUNK_SIZE = 300`, `CHUNK_OVERLAP = 50`
- `load_documents() -> list[Document]` returns `Document(page_content=p.read_text(encoding="utf-8"), metadata={"source": p.relative_to(EXAMPLES_DIR.parent).as_posix()})` for `p in sorted(DATA_DIR.glob("*.md"))`. Add a comment: 절대경로 대신 저장소 기준 상대경로를 저장한다.

Sections:
1. `=== 1. 문서 로딩 ===`
   - `docs = load_documents()`, then print `문서 {len(docs)}개 (타입: {type(docs[0]).__name__})`
   - per doc: `- {doc.metadata["source"]}  글자 수={len(doc.page_content)}`
   - `첫 문서 metadata: {docs[0].metadata}`
2. `=== 2. RecursiveCharacterTextSplitter로 분할 ===`
   - `splitter = RecursiveCharacterTextSplitter(chunk_size=CHUNK_SIZE, chunk_overlap=CHUNK_OVERLAP, add_start_index=True)`, `chunks = splitter.split_documents(docs)`
   - print `chunk_size={CHUNK_SIZE}, chunk_overlap={CHUNK_OVERLAP}` and `문서 {len(docs)}개 -> 청크 {len(chunks)}개`
   - per chunk (`enumerate`): `[{i:2d}] {Path(c.metadata["source"]).name} start_index={c.metadata["start_index"]} 길이={len(c.page_content)} | {c.page_content[:30]!r}`
   - per source: `{name}: 청크 {n}개`
3. `=== 3. 청크 하나와 겹침 확인 ===`
   - take the first two chunks of the first document (`a`, `b`) and print `--- 청크 0 전체 ---`, `a.page_content`, `--- 청크 1 전체 ---`, `b.page_content`
   - print `청크 0 끝 위치: {a.metadata["start_index"] + len(a.page_content)}, 청크 1 시작 위치: {b.metadata["start_index"]}`
   - print `겹친 글자 수: {max(0, a.metadata["start_index"] + len(a.page_content) - b.metadata["start_index"])}`

Capture: `uv run --project examples python scripts/run_examples.py ch03_rag/01_load_split.py --no-warmup` (LLM-free, so warm-up is unnecessary).

Before writing prose, verify library facts in a scratch script (not a repo file). Use them in prose only if confirmed:
- default separators: print `RecursiveCharacterTextSplitter()._separators`
- default `length_function` is `len` (character count)

**B. `book/src/SUMMARY.md`**: insert this block between the 2부 list and `# 부록`, with blank lines around it:
```
# 3부 RAG

- [문서 로딩과 분할](ch03_rag/01_load_split.md)
```

**C. `book/src/ch03_rag/01_load_split.md`**, titled `# 문서 로딩과 분할`:
- **개념: 왜 필요한가:**
  - The model cannot answer from documents it never saw (internal rules, personal notes). RAG retrieves relevant pieces and puts them into the prompt.
  - 3부 roadmap: this chapter loads and splits; the next embeds and searches; the last generates answers with an LCEL chain.
  - Why split: prompts have length limits, and small focused chunks make retrieval precise.
  - `Document` = `page_content` + `metadata`.
  - This book reads files with standard `pathlib` and builds `Document` directly instead of the `langchain-community` loaders, whose support has ended.
- **최소 코드:**
  - `### 샘플 문서`: the three files and their topics; they are original text written for this book, and the handbook is fiction. Show the handbook via a `text` fence containing only `{{#include ../../../examples/data/ch03_rag/03_company_handbook.md}}`.
  - `### 예제 코드`: path line, `python` include of the example, and bullets on:
    - repo-relative `source` metadata and why it is not absolute
    - `chunk_size`/`chunk_overlap` measured in characters
    - `add_start_index`
    - the separator order (only as verified)
    Then `실행 방법 (examples 디렉터리에서):` and the `bash` fence `uv run python ch03_rag/01_load_split.py`.
- **실제 출력:** say this chapter's example calls no LLM, so the same documents and library versions should produce the same result. Then the `text` include with `:2:`. Interpret from the actual `.out`: document count and lengths, total chunk count and per-document counts, how chunk boundaries fall (paragraph/heading boundaries or not), all lengths ≤ 300, and what section 3's overlap number shows (including if it is 0, explain why).
- **요점 정리:** 3–5 bullets.

Commit: `feat(03-01): chapter 3-1 (문서 로딩과 분할)`.
  </action>
  <verify>
- The runner prints `OK`. The key count on `outputs/ch03_rag/01_load_split.out` is `0`. `uv run --project examples python scripts/check_leaks.py outputs/ch03_rag` prints `CLEAN`.
- The `.out` contains `문서 3개`, `-> 청크`, `start_index=` and `examples/data/ch03_rag/01_langchain_intro.md`, and no `Traceback`.
- An independent scratch check with the same parameters confirms:
  - every chunk length ≤ 300
  - each of the 3 sources has ≥ 2 chunks
  - the total equals the count printed in the `.out`
  If a document yields fewer than 2 chunks, lengthen that document (still ≤ 1500 chars; allowed only before this task's commit) and re-capture.
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`.
- `uv run --project examples python scripts/check_book.py book/src/ch03_rag/01_load_split.md --html book/book` → `PASS`.
  </verify>
  <done>Chapter 3-1 shows real Document loading with repo-relative sources and a real chunking result (count, positions, lengths, overlap). It is linked under `# 3부 RAG`, passes check_book.py, and is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Chapter 3-2 (임베딩과 벡터스토어 검색) + 1부 1장 note + early determinism check</name>
  <files>examples/ch03_rag/02_embed_search.py, outputs/ch03_rag/02_embed_search.out, book/src/ch03_rag/02_embed_search.md, book/src/SUMMARY.md, book/src/ch01_basics/01_chat_model.md</files>
  <action>
**A. `examples/ch03_rag/02_embed_search.py`** (RAG-02 + RAG-03, no LLM). Module docstring `"""3부 2장: bge-m3로 임베딩하고 Chroma에 색인한 뒤 한국어 질의로 검색한다."""`.

Imports: `math`, `Path`, `Chroma` from `langchain_chroma`, `Document`, `RecursiveCharacterTextSplitter`, `get_embeddings` from `shared.config`.

Module level: the same `EXAMPLES_DIR`, `DATA_DIR`, `CHUNK_SIZE = 300`, `CHUNK_OVERLAP = 50` and `load_documents()` as 3-1, copied verbatim so both chapters split identically. Add `dot(a, b)` returning `sum(x * y for x, y in zip(a, b))`.

Sections:
1. `=== 1. 임베딩 모델 ===`
   - `embeddings = get_embeddings()`
   - print `모델: {embeddings.model_name}` and `model_kwargs: {embeddings.model_kwargs}`
   - `vec = embeddings.embed_query("재택근무는 일주일에 며칠 가능한가요?")`
   - print `차원: {len(vec)}`, `앞 5개 값: {[f"{x:.6f}" for x in vec[:5]]}` and `벡터 길이(L2 노름): {math.sqrt(dot(vec, vec)):.4f}`
2. `=== 2. 뜻이 가까우면 벡터도 가깝다 ===`
   - `base = "연차 휴가는 1년에 며칠인가요?"`, `similar = "휴가를 한 해에 며칠이나 쓸 수 있나요?"`, `different = "파이썬 리스트를 정렬하는 방법을 알려주세요."`
   - `b, s, d = embeddings.embed_documents([base, similar, different])`
   - print the three sentences
   - print `코사인 유사도(기준, 비슷한 뜻): {dot(b, s):.4f}` and `코사인 유사도(기준, 다른 주제): {dot(b, d):.4f}`, with a code comment: 정규화된 벡터라 내적이 곧 코사인 유사도
3. `=== 3. Chroma에 색인 ===`
   - `chunks = RecursiveCharacterTextSplitter(chunk_size=CHUNK_SIZE, chunk_overlap=CHUNK_OVERLAP, add_start_index=True).split_documents(load_documents())`
   - `vectorstore = Chroma.from_documents(chunks, embedding=embeddings, collection_name="ch03_rag")`, with a comment: persist_directory를 주지 않으면 메모리에만 저장된다
   - print `청크 {len(chunks)}개 색인 → 저장된 개수: {len(vectorstore.get()["ids"])}`. Only the length; ids are random.
4. `=== 4. 한국어 질의로 검색 ===`
   - `QUERIES = ["LCEL에서 파이프 연산자는 어떤 역할을 하나요?", "문서를 자를 때 청크를 겹치게 만드는 이유는?", "연차 휴가는 1년에 며칠 받을 수 있나요?"]`
   - for each: print `질의: {q}`, then for `rank, (doc, score)` in `enumerate(vectorstore.similarity_search_with_score(q, k=3), start=1)` print `  {rank}. {Path(doc.metadata["source"]).name} start_index={doc.metadata["start_index"]} 거리={score:.4f} | {doc.page_content[:30]!r}`

Capture: `uv run --project examples python scripts/run_examples.py ch03_rag/02_embed_search.py --no-warmup`.

**Noise contingency:** if the `.out` contains progress-bar text (`it/s]`, `Loading weights`, `%|`), the likely cause is `langchain_chroma` importing Hugging Face modules before `get_embeddings()` sets the env vars. Fix by moving `from shared.config import get_embeddings` above the third-party imports in this example, with a one-line comment. Re-capture and record the deviation. Rule 8 does not apply here; this is a code fix.

**Scratch verification before prose** (not a repo file), for the Chroma distance: re-embed a query and its rank-1 chunk with `get_embeddings()`, then compare the printed distance to `2 - 2*dot(q, c)`. If they match to 4 decimals, the prose may say the score is squared Euclidean distance between unit vectors (= 2 − 2×코사인 유사도, 작을수록 가깝다). Otherwise the prose says only "거리 값이며 작을수록 가깝다", if ascending order confirms it.

**B. `book/src/SUMMARY.md`**: add `- [임베딩과 벡터스토어 검색](ch03_rag/02_embed_search.md)` under the 3부 link from Task 2.

**C. `book/src/ch03_rag/02_embed_search.md`**, titled `# 임베딩과 벡터스토어 검색`:
- **개념: 왜 필요한가:**
  - To find chunks "about" a question, text must become numbers: an embedding model maps text to a vector, and similar meanings land close together.
  - A vector store keeps each chunk with its vector and returns the chunks nearest a query vector.
  - The documents are Korean, so this book runs the multilingual `BAAI/bge-m3` locally (on Apple Silicon via MPS, no server call).
  - Chroma runs embedded in the process; without a persist directory it lives only in memory, and indexing a few documents is fast, so each example rebuilds the index from scratch.
- **최소 코드:**
  - `### 임베딩 모델을 만드는 곳: shared/config.py`: include `../../../examples/shared/config.py:embeddings`. Explain:
    - the import happens inside the function, so 1·2부 examples never load torch
    - the env vars must be set before the import, or progress bars leak into the output
    - `normalize_embeddings`
    - automatic device choice
    - `**overrides`, same as `get_chat_model`
  - `### .env 설정 (선택)`: an `ini` fence with the line-range include of `examples/.env.example` starting at its blank-line-after comment line, i.e. the `# 임베딩 모델 설정` line. Find the number with `/usr/bin/grep -n "임베딩 모델 설정" examples/.env.example` and use `{{#include ../../../examples/.env.example:N:}}`. Explain that it works unset, and that the first run downloads the model to `~/.cache/huggingface` (several GB, once). Say the dependencies are already in `pyproject.toml`, so `uv sync` installs them (torch is large).
  - `### 예제 코드`: path, include, bullets on `embed_query` vs `embed_documents`, the dot product, `Chroma.from_documents`, and `similarity_search_with_score`. Say why only counts are printed (random ids) and why values are rounded. Add the `bash` run command `uv run python ch03_rag/02_embed_search.py`.
- **실제 출력:** say no LLM is called but values come from the embedding model, so the last digits may differ with other library versions or devices. Then the include. Interpret from the actual `.out`:
  - model and device, dimension 1024, and a norm of 1.0000 (normalization)
  - the two similarities and their gap
  - index count equal to the chunk count
  - for each query, which document ranked first, and the distance pattern (only as verified above)
- **요점 정리:** 3–5 bullets.

**D. `book/src/ch01_basics/01_chat_model.md`**: directly after the three `LLM_*` bullets, add one paragraph: `파일 끝의 \`EMBEDDING_MODEL\`/\`EMBEDDING_DEVICE\`는 [3부 RAG](../ch03_rag/02_embed_search.md)에서 쓰는 선택 설정이라 지금은 그대로 두면 된다.` Change nothing else in that chapter.

**E. Early determinism check** (rule 9): `uv run --project examples python scripts/run_examples.py ch03_rag/01_load_split.py ch03_rag/02_embed_search.py --no-warmup --outputs-dir "$SCRATCH/early"`. Key-count both scratch files, then `diff` each against the committed/working `outputs/ch03_rag/*.out`. Require byte-identical, applying rule 9 otherwise.

Commit: `feat(03-01): chapter 3-2 (임베딩과 벡터스토어 검색)`.
  </action>
  <verify>
- The runner prints `OK`. The key count on `outputs/ch03_rag/02_embed_search.out` is `0`.
- The `.out` contains:
  - `차원: 1024`, `'device': 'mps'` and `벡터 길이(L2 노름): 1.0000`
  - `저장된 개수:` with the same number as `청크 N개`
  - three `질의:` blocks
  - no `Traceback`, `it/s]`, `Loading weights` or `0x`
- In section 2, similarity(기준, 비슷한 뜻) > similarity(기준, 다른 주제); otherwise apply rule 8.
- In section 4, rank-1 sources are `01_langchain_intro.md`, `02_rag_guide.md` and `03_company_handbook.md` for the three queries respectively; otherwise apply rule 8.
- Early determinism diff: both files byte-identical.
- `git grep -n "persist_directory" -- 'examples/*.py' 'examples/**/*.py'` shows only the comment line, never an argument. `git grep -n "HuggingFaceEmbeddings(" -- examples ':!examples/.venv'` matches only `examples/shared/config.py`. `git grep -n "@tool" -- examples ':!examples/.venv'` matches only `examples/shared/tools.py`.
- `git status --porcelain` shows no untracked files except `.planning/` docs (in particular no Chroma or sqlite artifacts under `examples/`).
- `rm -rf book/book && mdbook build book 2>&1 | /usr/bin/grep -c ERROR` prints `0`.
- `uv run --project examples python scripts/check_book.py --html book/book` → 9× `PASS` (5 ch01 + 2 ch02 + 2 ch03).
- `/usr/bin/grep -c "LLM_BASE_URL" book/book/ch03_rag/02_embed_search.html` prints `0`, which confirms the `.env.example` line range. `/usr/bin/grep -c "EMBEDDING_MODEL" book/book/ch01_basics/01_chat_model.html` ≥ 1.
  </verify>
  <done>Chapter 3-2 shows real bge-m3 embeddings on mps (1024 dims, normalized), a meaningful similarity contrast, an ephemeral Chroma index, and three Korean queries retrieving their intended documents. 1부 1장 explains the new env lines. Both ch03 captures re-run byte-identically. 9 chapters pass. Committed locally, not pushed.</done>
</task>

</tasks>

<verification>
Plan-level checks (retrieval half of Phase 3):
1. `outputs/ch03_rag/01_load_split.out` shows Document loading and RecursiveCharacterTextSplitter chunk count and contents (success criterion 1, RAG-01)
2. `outputs/ch03_rag/02_embed_search.out` shows bge-m3 on mps with dimension 1024 (criterion 2, RAG-02) and Chroma search results for Korean queries (criterion 3, RAG-03)
3. Invariants: single embedding factory with lazy import, no persist dir, repo-relative sources, include-only chapters (9× PASS), byte-identical LLM-free re-capture
</verification>

<success_criteria>
- RAG-01, RAG-02, RAG-03 chapters exist locally with real, reproducible captures and pass the format gate
- `get_embeddings()`, the sample documents and chunk parameters are stable inputs for 03-02's RAG chain
- Nothing pushed yet; all work committed on local main
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag/03-01-SUMMARY.md`. Record, with no secret values, absolute paths, email addresses or ports:
- the resolved versions from Task 1 (for the reproducibility record) and whether `uv lock --project examples --check` passed
- the smoke-test stderr line count and whether the noise contingency was needed
- total chunk count and per-document chunk counts; section 3's overlap value
- the embedding device, the two similarity values, and each query's rank-1 source and distance
- whether the Chroma distance = 2 − 2×cosine check held (and so what the prose asserts)
- any rule-8 query revisions (old → new text) and the early determinism result (identical / flake re-run)
Before committing SUMMARY, check that `/usr/bin/grep -cE '/Users/[A-Za-z]|/private/|/tmp/claude' .planning/phases/03-rag/03-01-SUMMARY.md` prints `0`.
</output>
