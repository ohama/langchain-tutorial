---
phase: 01-book-infra-basics
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/check_book.py
  - examples/ch01_basics/01_chat_model.py
  - outputs/ch01_basics/01_chat_model.out
  - book/src/ch01_basics/01_chat_model.md
  - examples/ch01_basics/02_messages.py
  - outputs/ch01_basics/02_messages.out
  - book/src/ch01_basics/02_messages.md
  - examples/ch01_basics/03_prompt_templates.py
  - outputs/ch01_basics/03_prompt_templates.out
  - book/src/ch01_basics/03_prompt_templates.md
autonomous: true
requirements: [BASIC-01, BASIC-02, BASIC-03, INFRA-03, INFRA-07, INFRA-08]

must_haves:
  truths:
    - "Chapter '채팅 모델 호출하기' shows get_chat_model().invoke and .stream code and the real captured output, with stream chunk boundaries observable in the static output"
    - "Chapter '채팅 모델 호출하기' shows the reader how to copy examples/.env.example to .env and which three values to change, displaying .env.example and the shared/config.py settings/chat_model regions via {{#include}}"
    - "Chapter '메시지와 멀티턴 대화' shows System/Human/AI messages where the model answers a follow-up using information from an earlier turn, contrasted with the same follow-up sent without history"
    - "Chapter '프롬프트 템플릿' shows ChatPromptTemplate variables and FewShotChatMessagePromptTemplate expansion (formatted messages) and the model's real answers"
    - "Each of the three chapters has exactly the ## headings 개념: 왜 필요한가 / 최소 코드 / 실제 출력 / 요점 정리 in order, and every python/text/ini code block is a single {{#include}} line"
    - "All three .out files were produced by scripts/run_examples.py (hash header on line 1, masked, zero key occurrences)"
  artifacts:
    - path: "scripts/check_book.py"
      provides: "Chapter structure + include-only + rendered-HTML checker (stdlib)"
      min_lines: 60
    - path: "examples/ch01_basics/01_chat_model.py"
      provides: "BASIC-01 invoke + stream example"
      contains: "get_chat_model"
    - path: "examples/ch01_basics/02_messages.py"
      provides: "BASIC-02 multi-turn messages example"
      contains: "SystemMessage"
    - path: "examples/ch01_basics/03_prompt_templates.py"
      provides: "BASIC-03 ChatPromptTemplate + few-shot example"
      contains: "FewShotChatMessagePromptTemplate"
    - path: "outputs/ch01_basics/01_chat_model.out"
      provides: "Captured real output"
    - path: "outputs/ch01_basics/02_messages.out"
      provides: "Captured real output"
    - path: "outputs/ch01_basics/03_prompt_templates.out"
      provides: "Captured real output"
    - path: "book/src/ch01_basics/01_chat_model.md"
      provides: "Chapter 1 (replaces stub)"
      contains: "{{#include ../../../outputs/ch01_basics/01_chat_model.out:2:}}"
    - path: "book/src/ch01_basics/02_messages.md"
      provides: "Chapter 2 (replaces stub)"
      contains: "{{#include ../../../outputs/ch01_basics/02_messages.out:2:}}"
    - path: "book/src/ch01_basics/03_prompt_templates.md"
      provides: "Chapter 3 (replaces stub)"
      contains: "{{#include ../../../outputs/ch01_basics/03_prompt_templates.out:2:}}"
  key_links:
    - from: "book/src/ch01_basics/0N_*.md"
      to: "examples/ch01_basics/0N_*.py and outputs/ch01_basics/0N_*.out"
      via: "{{#include ../../../examples/...py}} and {{#include ../../../outputs/...out:2:}}"
      pattern: "\\{\\{#include \\.\\./\\.\\./\\.\\./(examples|outputs)/ch01_basics/"
    - from: "examples/ch01_basics/*.py"
      to: "examples/shared/config.py"
      via: "from shared.config import get_chat_model (never ChatOpenAI directly)"
      pattern: "from shared.config import"
    - from: "outputs/ch01_basics/*.out"
      to: "scripts/run_examples.py"
      via: "generated only by the runner (sha256 header line)"
      pattern: "^# source-sha256: "
---

<objective>
Write the first three basics chapters end to end through the real pipeline: example `.py`, then capture via `scripts/run_examples.py`, then chapter `.md` that pulls code and output in only via `{{#include}}`. Also add `scripts/check_book.py`, which mechanically enforces the chapter format and include-only rule so it can gate every later chapter.

Purpose: This proves the authoring half of the pipeline on real content and establishes the reference chapter style that 01-04 and later phases copy. Covers BASIC-01..03, documents INFRA-03 for readers, and enforces INFRA-07/08.
Output: `scripts/check_book.py`; 3 examples, 3 captured `.out`, 3 chapters (replacing stubs).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-book-infra-basics/01-RESEARCH.md
@.planning/phases/01-book-infra-basics/01-01-SUMMARY.md
@.planning/phases/01-book-infra-basics/01-02-SUMMARY.md
@examples/shared/config.py
@examples/.env.example
@scripts/run_examples.py
@book/src/SUMMARY.md
@book/src/introduction.md
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only. Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`. Do NOT push in this plan; 01-04 pushes.
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. Verify with `/usr/bin/grep -cF -- "$LITELLM_API_KEY" FILE` → `0` after `test -n "$LITELLM_API_KEY"`. Never `cat` raw example output outside the runner, and view `.out` files only after the count check.
3. **grep:** use `/usr/bin/grep` or `git grep` (the shell's `grep` wraps ugrep).
4. **Outputs are generated, never hand-edited.** If an output is unsatisfying, change the `.py` and re-run the runner. Do not edit `.out` files.
5. **Examples:** import the model only via `from shared.config import get_chat_model` (never construct `ChatOpenAI` directly). Keep files minimal, with no `load_dotenv`, no sys.path hacks and no `# ANCHOR` markers (whole-file includes would show them). Print short `=== 섹션 ===` headers so the static output is readable. Do not print timings, absolute paths or the key. Examples must exit 0; if an error is part of the lesson, catch it and print only `type(e).__name__`.
6. **Scratch:** temporary files go in your session scratchpad, never the repo. No venv at repo root.
</rules>

<chapter_template>
Every chapter `.md` replaces its stub entirely and follows this skeleton. The ## headings are exact and ordered, and `###` sub-sections are allowed inside them.

~~~markdown
# <SUMMARY.md title, no number>

## 개념: 왜 필요한가

<2–4 Korean paragraphs (평서체 "~다"): the problem this solves, key terms, how it fits the previous chapter.>

## 최소 코드

`examples/ch01_basics/0N_name.py`

```python
{{#include ../../../examples/ch01_basics/0N_name.py}}
```

<Short Korean bullet explanation of the important lines. Refer to identifiers in inline code only, never re-typed code blocks.>

실행 방법 (examples 디렉터리에서):

```bash
uv run python ch01_basics/0N_name.py
```

## 실제 출력

아래는 저자가 로컬 `flashnext` 모델로 실제 실행해 캡처한 결과다. LLM 응답이므로 여러분의 결과와 글자 단위로 같지 않을 수 있다.

```text
{{#include ../../../outputs/ch01_basics/0N_name.out:2:}}
```

<1–3 Korean sentences interpreting what the output shows.>

## 요점 정리

- <3–5 bullets>
~~~

Hard rules, enforced by `scripts/check_book.py`:
- Fenced blocks tagged `python`, `text` or `ini` contain exactly one non-blank line, and that line is an `{{#include ...}}`.
- `bash` blocks may contain only shell commands the reader types, never program output.
- Includes of `outputs/*.out` always use the `:2:` line range, which hides the hash header.
- A file that contains `# ANCHOR:` markers (only `examples/shared/config.py`) must be included by anchor name, e.g. `{{#include ../../../examples/shared/config.py:chat_model}}`.
- Include paths from `book/src/ch01_basics/` are `../../../examples/...` and `../../../outputs/...` (verified with mdBook 0.5.3).
</chapter_template>

<tasks>

<task type="auto">
  <name>Task 1: check_book.py gate + Chapter 1 (채팅 모델 호출하기: setup, invoke, stream)</name>
  <files>scripts/check_book.py, examples/ch01_basics/01_chat_model.py, outputs/ch01_basics/01_chat_model.out, book/src/ch01_basics/01_chat_model.md</files>
  <action>
**A. `scripts/check_book.py`** (stdlib only, runnable as `uv run --project examples python scripts/check_book.py [MD ...] [--html DIR]`):
- With no MD args, parse `book/src/SUMMARY.md` and check every linked page under a `ch*/` directory. Intro and appendix are excluded.
- For each chapter .md:
  1. The list of `## ` headings equals exactly `["개념: 왜 필요한가", "최소 코드", "실제 출력", "요점 정리"]`.
  2. Every fenced block tagged `python`/`text`/`ini` has exactly one non-blank line matching `^\{\{#include (\S+?)(:[A-Za-z_][\w-]*|:\d*:\d*)?\}\}$`, and the resolved target file exists relative to the .md's directory.
  3. At least one include targets `examples/**.py` and at least one targets `outputs/**.out`. Every `outputs/` include ends with `:2:`.
  4. If an included file contains `ANCHOR:` and the include has no anchor suffix, fail.
  5. Fail if the .md still contains `이 장은 작성 중입니다`.
- `--html DIR` (default `book/book` if it exists): for each checked chapter, read `DIR/<same rel path>.html` and fail if it contains `{{#include`, `ANCHOR`, or `source-sha256`.
- Print `PASS <md>` or `FAIL <md>: <reason>` lines, and exit 1 on any failure.

**B. `examples/ch01_basics/01_chat_model.py`** (BASIC-01):
- `model = get_chat_model()`
- `=== invoke ===`: `response = model.invoke("LangChain을 한 문장으로 소개해줘.")`, then print `type(response).__name__` and `response.content`, and optionally `response.usage_metadata` (token counts).
- `=== stream ===`: iterate `model.stream("하늘이 파란 이유를 두 문장으로 설명해줘.")`, printing each `chunk.content` with `end="", flush=True`, and collect the chunks. Then print a newline, the chunk count, and `[repr(c.content) for c in chunks[:5]]`, so chunk boundaries are visible in a static file (research: a plain stream capture looks identical to invoke otherwise).

Capture: `uv run --project examples python scripts/run_examples.py ch01_basics/01_chat_model.py` must print `OK`.

**C. `book/src/ch01_basics/01_chat_model.md`** per `<chapter_template>`. Specific content:
- 개념: why a chat model abstraction exists (the same `invoke`/`stream` interface regardless of provider). This book uses `ChatOpenAI(base_url=...)` against an OpenAI-compatible local server. invoke returns the whole response at once, while stream returns pieces as they are generated. Add a short note that the first request can take tens of seconds on a cold server (warm-up).
- 최소 코드 starts with `### 준비: .env 설정`:
  - bash block `cd examples`, `cp .env.example .env`, `uv sync`
  - an `ini` include of `../../../examples/.env.example`
  - a Korean explanation of the three variables (the reader only changes these)
  - `### 설정을 읽는 곳: shared/config.py` with `python` includes of `../../../examples/shared/config.py:settings` and `:chat_model`, explaining that every example creates its model only through this file (`temperature=0`, `timeout=120`)
  - then `### 예제 코드`, which includes the example .py and the run command
- 실제 출력: include `.out:2:`, then interpret the invoke vs. stream difference and the chunk count/boundaries.
- 요점 정리: 3–5 bullets.
  </action>
  <verify>
- `test -n "$LITELLM_API_KEY" && /usr/bin/grep -cF -- "$LITELLM_API_KEY" outputs/ch01_basics/01_chat_model.out` prints `0`; `head -1 outputs/ch01_basics/01_chat_model.out` matches `# source-sha256: <64 hex>`
- `/usr/bin/grep -c "ChatOpenAI" examples/ch01_basics/01_chat_model.py` prints `0`
- `mdbook build book 2>&1 | tee "$SCRATCH/b.log"; /usr/bin/grep -c "ERROR" "$SCRATCH/b.log"` prints `0`
- `uv run --project examples python scripts/check_book.py book/src/ch01_basics/01_chat_model.md` prints `PASS` and exits 0
- Negative check of the gate: `uv run --project examples python scripts/check_book.py book/src/ch01_basics/02_messages.md` exits 1 (it is still a stub)
- `uv run --project examples python scripts/check_leaks.py outputs/ch01_basics book/src/ch01_basics/01_chat_model.md` prints `CLEAN`
  </verify>
  <done>check_book.py enforces headings, include-only blocks, `:2:` output includes, anchor usage, no stubs, and clean rendered HTML. Chapter 1 passes it, shows reader setup via the included .env.example/config.py regions, and shows real invoke+stream output with visible chunk boundaries. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Chapter 2 (메시지와 멀티턴 대화)</name>
  <files>examples/ch01_basics/02_messages.py, outputs/ch01_basics/02_messages.out, book/src/ch01_basics/02_messages.md</files>
  <action>
**`examples/ch01_basics/02_messages.py`** (BASIC-02), using `from langchain_core.messages import SystemMessage, HumanMessage, AIMessage`:
- `=== 1. 대화 기록과 함께 ===`:
  - `messages = [SystemMessage("너는 간결하게 한국어로 답하는 비서다."), HumanMessage("안녕! 내 이름은 민수고, 파이썬을 배우고 있어.")]`
  - `ai = model.invoke(messages)`, then `messages.append(ai)` (the returned object is an `AIMessage`)
  - `messages.append(HumanMessage("내 이름이 뭐였고, 무엇을 배우고 있다고 했지?"))` and invoke again
  - print every message in the final history as `[{type(m).__name__}] {m.content}`, plus the final answer
- `=== 2. 대화 기록 없이 같은 질문 ===`: `model.invoke([HumanMessage("내 이름이 뭐였고, 무엇을 배우고 있다고 했지?")])`, then print the answer. This shows the model has no memory of its own; the history list *is* the memory.
- Optionally `=== 3. 직접 만든 AIMessage ===`: construct `AIMessage("...")` by hand in the history to show that roles are just data.

Capture with `uv run --project examples python scripts/run_examples.py ch01_basics/02_messages.py`. If section 2's answer happens to guess correctly (unlikely), adjust the prompt and re-capture. Do not edit the .out.

**`book/src/ch01_basics/02_messages.md`** per `<chapter_template>`:
- 개념: the three roles, a statelessness explanation (the model is stateless, so the app resends the history each call), and a note that later chapters (LangGraph checkpointer) automate this.
- 실제 출력: point at the contrast between section 1 and section 2.
  </action>
  <verify>
- The runner prints `OK ch01_basics/02_messages.py`; `/usr/bin/grep -cF -- "$LITELLM_API_KEY" outputs/ch01_basics/02_messages.out` prints `0`
- The .out contains `SystemMessage`, `HumanMessage` and `AIMessage`, and section 1's final answer mentions 민수 (`/usr/bin/grep -c "민수" outputs/ch01_basics/02_messages.out` ≥ 2)
- `mdbook build book` log has no `ERROR`; `uv run --project examples python scripts/check_book.py book/src/ch01_basics/02_messages.md` → PASS
  </verify>
  <done>The chapter shows a real multi-turn conversation using System/Human/AI messages with remembered context, contrasted with the history-less call, and passes check_book.py. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Chapter 3 (프롬프트 템플릿: variables + few-shot)</name>
  <files>examples/ch01_basics/03_prompt_templates.py, outputs/ch01_basics/03_prompt_templates.out, book/src/ch01_basics/03_prompt_templates.md</files>
  <action>
**`examples/ch01_basics/03_prompt_templates.py`** (BASIC-03), using `from langchain_core.prompts import ChatPromptTemplate, FewShotChatMessagePromptTemplate`:
- `=== 1. 변수가 있는 템플릿 ===`:
  - `prompt = ChatPromptTemplate.from_messages([("system", "너는 {level} 수준의 학습자를 돕는 튜터다. 두 문장 이내로 답하라."), ("human", "{topic}을(를) 설명해줘.")])`
  - print `prompt.input_variables`
  - `value = prompt.invoke({"level": "초급", "topic": "재귀 함수"})`, then print each formatted message (`type(m).__name__`, `m.content`), showing substitution without calling the LLM
  - `answer = model.invoke(value)`, then print `answer.content`
  - Do NOT use `|` here. Chapter 4 introduces it; mention that in the prose.
- `=== 2. few-shot 템플릿 ===` (verified in research):
  - `examples = [{"input": "행복", "output": "슬픔"}, {"input": "크다", "output": "작다"}]`
  - `example_prompt = ChatPromptTemplate.from_messages([("human", "{input}"), ("ai", "{output}")])`
  - `few_shot = FewShotChatMessagePromptTemplate(example_prompt=example_prompt, examples=examples)`
  - `final_prompt = ChatPromptTemplate.from_messages([("system", "입력된 단어의 반대말만 한 단어로 답하라."), few_shot, ("human", "{input}")])`
  - print the expanded messages for `{"input": "빠르다"}` (showing the few-shot pairs became Human/AI messages)
  - for each of `["빠르다", "밝다", "무겁다"]`, run `model.invoke(final_prompt.invoke({"input": w}))` and print `w -> content`

Capture with the runner.

**`book/src/ch01_basics/03_prompt_templates.md`** per `<chapter_template>`:
- 개념: why templates (separating fixed instructions from variable input, and reuse) and what few-shot is (teaching an output format by example).
- 실제 출력: point out the formatted messages and the few-shot answers following the one-word format.
  </action>
  <verify>
- The runner prints `OK`; `/usr/bin/grep -cF -- "$LITELLM_API_KEY" outputs/ch01_basics/03_prompt_templates.out` prints `0`
- The .out contains `input_variables`-style output listing `level` and `topic`, and three `->` answer lines
- `/usr/bin/grep -c " | " examples/ch01_basics/03_prompt_templates.py` prints `0` (no LCEL pipe yet)
- `mdbook build book` log has no `ERROR`
- `uv run --project examples python scripts/check_book.py book/src/ch01_basics/01_chat_model.md book/src/ch01_basics/02_messages.md book/src/ch01_basics/03_prompt_templates.md` → 3× PASS
- `uv run --project examples python scripts/check_leaks.py outputs/ch01_basics book/src/ch01_basics book/book/ch01_basics` → CLEAN
  </verify>
  <done>The chapter shows variable substitution and few-shot expansion with real model answers. All three chapters pass check_book.py and the leak scan. Committed (not pushed).</done>
</task>

</tasks>

<verification>
- `uv run --project examples python scripts/check_book.py book/src/ch01_basics/0{1,2,3}_*.md` → all PASS
- `git ls-files outputs/ch01_basics` lists exactly 3 `.out` files, each starting with `# source-sha256:`
- `git grep -l -F -e "$LITELLM_API_KEY"` (after `test -n`) prints nothing
- `git grep -n "ChatOpenAI(" -- examples` matches only `examples/shared/config.py`
</verification>

<success_criteria>
- BASIC-01, BASIC-02, BASIC-03 each have a chapter with real captured output
- INFRA-03 reader instructions exist in chapter 1, pulled from the actual `.env.example`/config.py
- INFRA-07/08 are mechanically enforced by `scripts/check_book.py` and pass for chapters 1–3
</success_criteria>

<output>
After completion, create `.planning/phases/01-book-infra-basics/01-03-SUMMARY.md`. Note the per-example runtimes the runner reported and any prompt changes made to get a meaningful output.
</output>
