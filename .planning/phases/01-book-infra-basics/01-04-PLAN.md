---
phase: 01-book-infra-basics
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - examples/ch01_basics/04_lcel_runnables.py
  - outputs/ch01_basics/04_lcel_runnables.out
  - book/src/ch01_basics/04_lcel_runnables.md
  - examples/ch01_basics/05_structured_output.py
  - outputs/ch01_basics/05_structured_output.out
  - book/src/ch01_basics/05_structured_output.md
autonomous: true
requirements: [BASIC-04, BASIC-05, INFRA-01, INFRA-02, INFRA-06, INFRA-07, INFRA-08]

must_haves:
  truths:
    - "Chapter 'LCEL로 체인 만들기' shows a prompt | model | StrOutputParser chain, RunnableLambda, RunnableParallel, batch and stream with real captured output"
    - "Chapter '구조화 출력' shows a Pydantic schema returned as a typed object via get_structured_model (function_calling, strict=False) with real output, and explains why the default json_schema method returns HTTP 500 on this local server and how function_calling avoids it"
    - "scripts/check_book.py passes for all 5 chapters (headings in order, include-only code/output, :2: output includes, no stubs, clean rendered HTML)"
    - "check_leaks.py reports CLEAN for outputs/, book/src/, examples/ and the built book, and --secrets-only reports CLEAN for all tracked files and the full git history"
    - "After push, the latest Actions run succeeded, and the live site serves all 5 chapter pages each containing text from its captured .out"
  artifacts:
    - path: "examples/ch01_basics/04_lcel_runnables.py"
      provides: "BASIC-04 LCEL example"
      contains: "RunnableParallel"
    - path: "examples/ch01_basics/05_structured_output.py"
      provides: "BASIC-05 structured output example"
      contains: "get_structured_model"
    - path: "outputs/ch01_basics/04_lcel_runnables.out"
      provides: "Captured real output"
    - path: "outputs/ch01_basics/05_structured_output.out"
      provides: "Captured real output"
    - path: "book/src/ch01_basics/04_lcel_runnables.md"
      provides: "Chapter 4 (replaces stub)"
      contains: "{{#include ../../../outputs/ch01_basics/04_lcel_runnables.out:2:}}"
    - path: "book/src/ch01_basics/05_structured_output.md"
      provides: "Chapter 5 (replaces stub)"
      contains: "{{#include ../../../examples/shared/config.py:structured}}"
  key_links:
    - from: "examples/ch01_basics/05_structured_output.py"
      to: "examples/shared/config.py"
      via: "get_structured_model(schema), which carries method=function_calling, strict=False"
      pattern: "get_structured_model\\("
    - from: "book/src/ch01_basics/05_structured_output.md"
      to: "examples/shared/config.py"
      via: "anchor include :structured shows the convention's source"
      pattern: "config.py:structured"
    - from: "git push origin main"
      to: "https://ohama.github.io/langchain-tutorial/ch01_basics/05_structured_output.html"
      via: "deploy.yml Actions run"
      pattern: "deploy-pages"
---

<objective>
Finish the basics part with chapters 4 (LCEL/Runnables) and 5 (structured output + the speculative-decoding 500 error), then run the whole-phase gate: format check for all chapters, secret/path leak scan over outputs and the built book, git hygiene, push, a successful Actions run, and live verification that every chapter page shows its captured output.

Purpose: BASIC-04/05 complete the reader-facing content. The final gate is the only point where the phase's public promise is checked as a whole: real outputs, no leaks, deployed.
Output: 2 examples, 2 `.out`, 2 chapters; pushed main; verified live site.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-book-infra-basics/01-RESEARCH.md
@.planning/phases/01-book-infra-basics/01-03-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@examples/shared/config.py
@scripts/run_examples.py
@scripts/check_book.py
@book/src/ch01_basics/01_chat_model.md
@book/src/ch01_basics/03_prompt_templates.md

Read STACK.md's "구조화 출력" section and PITFALLS.md "Pitfall 2" for the 500-error root cause (default `method="json_schema"`, and also strict `function_calling`, trigger schema-constrained decoding, which fails with HTTP 500 while the local MLX server runs speculative decoding/MTP; `method="function_calling", strict=False` works). Use `book/src/ch01_basics/01_chat_model.md` as the style reference for tone and structure.
</context>

<rules>
1. **Commit attribution:** every commit message ends with exactly:
   ```
   Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01B64YhxKTVVi4wWuK9QnyYq
   ```
   Stage explicit paths only. Never stage `.claude/`, `.env`, `.venv/`, `book/book/`, `__pycache__/`.
2. **Secrets:** never print or write `$LITELLM_API_KEY`'s value. Use count-only checks after `test -n "$LITELLM_API_KEY"`. In the error demo, never print `str(e)` or tracebacks; they can include request details.
3. **grep:** use `/usr/bin/grep` or `git grep`.
4. **Outputs are generated, never hand-edited.** Change the `.py` and re-run the runner instead.
5. **Examples:** the model comes only from `shared.config`. No `# ANCHOR` markers, no `load_dotenv`, no timings or paths printed. Examples exit 0.
6. **Chapter format:** follow the `<chapter_template>` from `01-03-PLAN.md` exactly: `##` headings `개념: 왜 필요한가` / `최소 코드` / `실제 출력` / `요점 정리`; python/text/ini blocks contain a single `{{#include}}` line; `outputs` includes use `:2:`; config.py only by anchor. `scripts/check_book.py` enforces this.
7. **Scratch / waiting:** temp files go in your session scratchpad. No `sleep`; use `gh run watch` and `curl --retry`.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Chapter 4 (LCEL로 체인 만들기)</name>
  <files>examples/ch01_basics/04_lcel_runnables.py, outputs/ch01_basics/04_lcel_runnables.out, book/src/ch01_basics/04_lcel_runnables.md</files>
  <action>
**`examples/ch01_basics/04_lcel_runnables.py`** (BASIC-04), importing `ChatPromptTemplate`, `StrOutputParser` (`langchain_core.output_parsers`) and `RunnableLambda`, `RunnableParallel` (`langchain_core.runnables`):
- `=== 1. 파이프로 연결 ===`: `chain = prompt | model | StrOutputParser()`, where the prompt is e.g. `"{topic}을(를) 한 문장으로 설명해줘."`. Print `type(chain).__name__` (RunnableSequence) and `chain.invoke({"topic": "벡터 데이터베이스"})`. The result is a plain `str`, not an AIMessage, so print `type(result).__name__` too.
- `=== 2. RunnableLambda ===`: wrap a plain Python function, e.g. `RunnableLambda(lambda text: text.strip().upper())` for an English input, or a function returning `{"topic": ...}` to adapt raw strings into the prompt's dict. Use it in a chain `to_input | prompt | model | StrOutputParser()` invoked with a bare string.
- `=== 3. RunnableParallel ===`: `RunnableParallel(summary=summary_prompt | model | StrOutputParser(), keywords=keyword_prompt | model | StrOutputParser())` on one short Korean paragraph. Print the result dict keys and each value.
- `=== 4. batch ===`: `chain.batch([{"topic": t} for t in ["임베딩", "토큰", "프롬프트"]])`, printing `topic -> answer` lines to show order is preserved.
- `=== 5. stream ===`: `for chunk in chain.stream({"topic": "스트리밍"})`, printing chunks with `end=""`, then the chunk count. Note the chunks are `str` because of StrOutputParser.

Capture with `uv run --project examples python scripts/run_examples.py ch01_basics/04_lcel_runnables.py`.

**`book/src/ch01_basics/04_lcel_runnables.md`**:
- 개념: LCEL/Runnable, where every component (prompt, model, parser, plain function) shares `invoke`/`batch`/`stream`, so `|` composes them. Reference chapter 3, where we called `model.invoke(prompt.invoke(...))` by hand, and show this is the same thing.
- 최소 코드: optional `###` sub-headings explaining sections 1–5 in bullets. Code comes only via the single include.
- 실제 출력: interpret the str result type, the parallel dict, batch ordering, and stream chunks.
  </action>
  <verify>
- The runner prints `OK`; `/usr/bin/grep -cF -- "$LITELLM_API_KEY" outputs/ch01_basics/04_lcel_runnables.out` prints `0`
- The .out contains `RunnableSequence`, `summary` and `keywords`, and 3 batch `->` lines
- `mdbook build book` log has no `ERROR`; `uv run --project examples python scripts/check_book.py book/src/ch01_basics/04_lcel_runnables.md` → PASS
  </verify>
  <done>The chapter shows pipe composition, RunnableLambda, RunnableParallel, batch and stream with real output and passes check_book.py. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Chapter 5 (구조화 출력 + 500 에러 원인과 해결)</name>
  <files>examples/ch01_basics/05_structured_output.py, outputs/ch01_basics/05_structured_output.out, book/src/ch01_basics/05_structured_output.md</files>
  <action>
**`examples/ch01_basics/05_structured_output.py`** (BASIC-05):
```python
from pydantic import BaseModel, Field
from shared.config import get_chat_model, get_structured_model

class Person(BaseModel):
    """텍스트에서 추출한 인물 정보"""
    name: str = Field(description="이름")
    age: int = Field(description="나이")
    hobbies: list[str] = Field(description="취미 목록")
```
- `=== 1. function_calling 방식 (이 책의 규약) ===`: `structured = get_structured_model(Person)`; `result = structured.invoke("김철수는 30살이고 등산과 사진 찍기를 좋아한다.")`. Print `type(result).__name__`, `repr(result)`, `result.age + 1` (proves it is a real int), and `result.model_dump_json(indent=2)`.
- Second input for robustness, e.g. "이영희(27)는 요즘 요리에 빠져 있다." Print `repr`.
- `=== 2. 기본값(json_schema)으로 호출하면? ===` is the intentional anti-pattern demo, the only place outside config.py that calls `with_structured_output` directly:
  `default_structured = get_chat_model().with_structured_output(Person)`. Wrap `invoke` in `try/except Exception as e:` and print only `실패: {type(e).__name__}` and `status_code={getattr(e, "status_code", None)}`. On success, print `성공: {type(r).__name__}` instead. Never print `str(e)`.

Capture with the runner. Before writing prose, look at section 2's actual result (after the count-only key check):
- If it shows an HTTP 500 (expected per STACK.md), the chapter explains that result.
- If it succeeds (server config changed), the prose must honestly say that in this capture the default worked, while still explaining why the book standardizes on `function_calling, strict=False` (speculative decoding setups break schema-constrained decoding; portability). Never claim an error the output does not show.

**`book/src/ch01_basics/05_structured_output.md`**:
- 개념: why structured output matters (programs need fields, not prose) and why Pydantic.
- 최소 코드:
  - `### 이 책의 규약: get_structured_model` with an include of `../../../examples/shared/config.py:structured`
  - explain `method="function_calling"` (the schema is sent as a tool definition and the model fills in arguments) and `strict=False`
  - then `### 예제 코드` with the example include and run command
- 실제 출력: interpret the typed object (`result.age + 1`), then add `### 트러블슈팅: speculative decoding과 500 에러`. Cover the cause: `with_structured_output`'s default `json_schema` (and strict function_calling) asks the server for schema-constrained decoding, and this local MLX server's speculative decoding (MTP) does not support that, so it answers HTTP 500. Cover the fix: `method="function_calling", strict=False`, centralized in config.py so every chapter inherits it. Reference the actual status code printed in section 2. Use plain markdown, no admonition preprocessor.
- 요점 정리: include "구조화 출력은 항상 `get_structured_model`로".
  </action>
  <verify>
- The runner prints `OK`; `/usr/bin/grep -cF -- "$LITELLM_API_KEY" outputs/ch01_basics/05_structured_output.out` prints `0`
- The .out contains `Person(` and `"hobbies"`, and a line starting with `실패:` or `성공:`. It does not contain `Traceback`.
- `git grep -n "with_structured_output" -- examples ':!examples/.venv'` matches only `examples/shared/config.py` and the single demo line in `05_structured_output.py`
- `git grep -n "json_schema" -- examples/shared/config.py` prints nothing (the convention never uses the default)
- `mdbook build book` log has no `ERROR`; `uv run --project examples python scripts/check_book.py book/src/ch01_basics/05_structured_output.md` → PASS
- `/usr/bin/grep -c "500" book/src/ch01_basics/05_structured_output.md` ≥ 1 and `/usr/bin/grep -c "function_calling" book/src/ch01_basics/05_structured_output.md` ≥ 1
  </verify>
  <done>The chapter shows real typed Pydantic output via the function_calling convention, a truthful captured demo of the default method, and an explanation of the 500 error's cause and fix. It passes check_book.py. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-phase gate: format, leak scan, git hygiene, push, Actions + live verification</name>
  <files>(verification only; fixes go to whichever Phase 1 file a check implicates, e.g. book/src/ch01_basics/*.md, examples/ch01_basics/*.py with re-captured outputs/ch01_basics/*.out)</files>
  <action>
Run each gate in order, fix any failure at its source (re-capture outputs via the runner, never hand-edit), and re-run until all pass:

1. **Fresh build:** `rm -rf book/book && mdbook build book 2>&1 | tee "$SCRATCH/final_build.log"`. Require exit 0 and zero `ERROR`/`WARN` lines.
2. **Format / include-only (INFRA-07/08):** `uv run --project examples python scripts/check_book.py --html book/book`, with no MD args so it runs all 5 chapters from SUMMARY.md. Require 5× PASS and exit 0.
3. **Leak scan (INFRA-06):**
   - `uv run --project examples python scripts/check_leaks.py outputs book/src examples book/book` → `CLEAN`. This full mode covers the key literal, sk-/Bearer tokens and home paths. If a hit lands in a vendored mdBook theme asset, inspect the reported line kind/location only. Confirm it is not a real leak, then document it in SUMMARY and re-run on `book/book/**/*.html` + search index files.
   - `git ls-files -z | xargs -0 uv run --project examples python scripts/check_leaks.py --secrets-only` → `CLEAN`
   - `git log -p --all | uv run --project examples python scripts/check_leaks.py --secrets-only -` → `CLEAN`
4. **Git hygiene:**
   - `git ls-files | /usr/bin/grep -E '(^|/)\.env$|(^|/)\.venv/|^\.claude/|^book/book/|__pycache__|\.pytest_cache'` prints nothing
   - `test ! -e .venv && test ! -e .pytest_cache && test ! -e main.py`
   - `git status --porcelain` shows no untracked files other than ignored ones (`.claude/` must appear neither untracked nor staged, because it is ignored)
   - `git ls-files outputs/ch01_basics | wc -l` = 5, each with a `# source-sha256:` first line
5. **Model-factory invariant (INFRA-04):** `git grep -n "ChatOpenAI(" -- examples` matches only `examples/shared/config.py`.
6. **Push:** commit any fixes, then `git push origin main`. Get `RUN_ID` via `gh run list --repo ohama/langchain-tutorial --workflow deploy.yml --branch main --limit 1 --json databaseId,headSha --jq '.[0]'` and confirm headSha equals `git rev-parse HEAD`. Then `gh run watch "$RUN_ID" --repo ohama/langchain-tutorial --exit-status`.
7. **Live verification (INFRA-01/02):** for each of the 5 chapters, fetch `https://ohama.github.io/langchain-tutorial/ch01_basics/<name>.html` with `curl -sSf --retry 12 --retry-delay 10 --retry-all-errors`. Assert with a short python snippet run from the scratchpad (not a repo file) that the page contains a distinctive line from line 2+ of the matching `.out`, compared after `html.unescape` on the page. Also assert the page contains neither `{{#include` nor `이 장은 작성 중입니다`. Also re-check that the index contains `1부 기초` and `부록`, and the 404 page returns 404 with `/langchain-tutorial/` asset paths.
  </action>
  <verify>
- `scripts/check_book.py --html book/book` → 5× PASS, exit 0
- All three check_leaks invocations print `CLEAN`
- `gh run view "$RUN_ID" --repo ohama/langchain-tutorial --json conclusion,headSha --jq '.conclusion'` prints `success`, and headSha matches local HEAD
- The live check snippet prints `LIVE_OK <name>` for all 5 chapters, and index/404 checks pass
- `git status --porcelain` is empty after the final commit
  </verify>
  <done>All five basics chapters pass the format gate. Outputs, sources, built book, tracked files and history are leak-free. The final commit is pushed, the Actions run for that exact SHA succeeded, and the live site serves all five chapters with their real captured outputs.</done>
</task>

</tasks>

<verification>
Phase 1 success criteria, end to end:
1. The live Pages URL shows the Korean mdBook with 소개 / 1부 기초 (5) / 부록 (INFRA-01)
2. The Actions run for HEAD succeeded with build-only steps (INFRA-02)
3. `.env.example` + `shared/config.py` are the reader's only knobs, documented in chapter 1 and enforced by `git grep "ChatOpenAI("` (INFRA-03/04)
4. `.out` files are produced by the one-command runner and pass the leak scan (INFRA-05/06)
5. `check_book.py` PASS ×5 means include-only and four-part format (INFRA-07/08, BASIC-01..05)
</verification>

<success_criteria>
- BASIC-04 and BASIC-05 chapters are published with real outputs
- The whole-phase gate (format, leaks, hygiene) passes and the deployed site for the pushed SHA is verified live
</success_criteria>

<output>
After completion, create `.planning/phases/01-book-infra-basics/01-04-SUMMARY.md`. Record the section-2 result of the structured-output demo (status code or success), the final run ID, the live URLs checked, and any leak-scan false positives, with no secret values.
</output>
