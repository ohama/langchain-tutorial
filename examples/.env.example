# LLM 연결 설정 — 이 파일을 .env로 복사한 뒤 자신의 환경에 맞게 바꾸세요.
# OpenAI 호환 API 주소 (LiteLLM, Ollama의 /v1, vLLM 등)
LLM_BASE_URL=http://127.0.0.1:4000/v1
# 사용할 모델 이름(별칭)
LLM_MODEL=flashnext
# API 키. 인증이 없는 로컬 서버라면 비워 두어도 됩니다.
# 셸 환경변수를 참조할 수도 있습니다. 예: LLM_API_KEY=${MY_KEY_ENV}
LLM_API_KEY=
